---
phase: 260510-j5k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/audit-changes.jsonl
autonomous: true
requirements:
  - 260510-oua
must_haves:
  truths:
    - "Ouagadougou question's world-capitals score is 70 in question_categories"
    - "No other category scores for that question are altered"
    - "An audit-changes.jsonl entry records the change with batch_id 260510-oua"
  artifacts:
    - path: "data/audit-changes.jsonl"
      provides: "Append-only audit row for the score bump"
      contains: "260510-oua"
  key_links:
    - from: "Supabase question_categories row"
      to: "data/audit-changes.jsonl entry"
      via: "matching question_id + slug=world-capitals + new_score=70"
      pattern: "world-capitals.*70"
---

<objective>
Bump the Ouagadougou question's `world-capitals` relevance score from 45 to 70 in the Supabase `question_categories` table, and record the change in `data/audit-changes.jsonl`. Deferred from phase 999.23 cousin/cat audit.

Purpose: Improve category-relevance ranking so the Ouagadougou question surfaces correctly under `world-capitals`.
Output: One updated row in Supabase + one append to audit-changes.jsonl.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.23-cousin-cat-audit/999.23-VERIFICATION.md
@data/audit-changes.jsonl

<interfaces>
Audit row shape (one JSON object per line in data/audit-changes.jsonl):
```json
{"ts":"<ISO-8601>","batch_id":"260510-oua","question_id":"<uuid>","op":"update","slug":"world-capitals","prev_score":45,"new_score":70,"reason":"260510-oua: Ouagadougou is canonical capital — score bumped from 45 to 70 per ROADMAP §C1","cousin_reason":null,"chain_ancestor":false}
```

Supabase table: `question_categories(question_id uuid, category_slug text, score int)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Find the Ouagadougou question and confirm current row</name>
  <files>(read-only Supabase query)</files>
  <action>
    Use the Supabase MCP (`mcp__supabase__*`) if available; otherwise run `supabase db query --linked` with read-only SELECTs.

    Step 1 — find the question id:
    ```sql
    SELECT id, text
    FROM questions
    WHERE text ILIKE '%Ouagadougou%'
       OR text ILIKE '%capital of Burkina Faso%';
    ```
    Expect exactly one row. If zero or more than one, STOP and report — do not guess.

    Step 2 — confirm the current join row:
    ```sql
    SELECT question_id, category_slug, score
    FROM question_categories
    WHERE question_id = '<id-from-step-1>'
      AND category_slug = 'world-capitals';
    ```
    Expect one row with `score = 45`. If `score` is not 45, STOP and report the actual value — do not overwrite blindly.

    Record the question_id and current score for use in Task 2 and Task 3.
  </action>
  <verify>
    <automated>Step 1 returns exactly one row; Step 2 returns one row with score=45.</automated>
  </verify>
  <done>question_id known and current world-capitals score confirmed = 45.</done>
</task>

<task type="auto">
  <name>Task 2: UPDATE world-capitals score to 70</name>
  <files>(Supabase write)</files>
  <action>
    Run ONE targeted UPDATE — do not touch other rows for this question.

    ```sql
    UPDATE question_categories
    SET score = 70
    WHERE question_id = '<id-from-task-1>'
      AND category_slug = 'world-capitals'
      AND score = 45;
    ```

    The `AND score = 45` guard is required (optimistic check — fails safely if the row already moved).

    Then re-SELECT the row to confirm `score = 70`. Also SELECT all rows for this question to confirm no other category scores changed:
    ```sql
    SELECT category_slug, score
    FROM question_categories
    WHERE question_id = '<id-from-task-1>'
    ORDER BY score DESC, category_slug;
    ```

    Do NOT create any file under `supabase/migrations/` — this is a data fix, matching prior quick-task convention (260424-uju, 260426-myq, 260503-kxb).
  </action>
  <verify>
    <automated>UPDATE affects exactly 1 row; post-UPDATE SELECT returns score=70 for world-capitals; no other category_slug scores changed.</automated>
  </verify>
  <done>world-capitals score = 70 in Supabase; other category scores untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Append audit row to data/audit-changes.jsonl</name>
  <files>data/audit-changes.jsonl</files>
  <action>
    Append exactly ONE line (no trailing newline beyond the final `\n`) matching the shape in &lt;interfaces&gt;:

    ```json
    {"ts":"<now ISO-8601 UTC>","batch_id":"260510-oua","question_id":"<uuid>","op":"update","slug":"world-capitals","prev_score":45,"new_score":70,"reason":"260510-oua: Ouagadougou is canonical capital of Burkina Faso — score bumped from 45 to 70 per ROADMAP §C1","cousin_reason":null,"chain_ancestor":false}
    ```

    Use the question_id captured in Task 1 and the current ISO-8601 UTC timestamp. Append only — never rewrite earlier lines.
  </action>
  <verify>
    <automated>tail -1 data/audit-changes.jsonl | grep -c '"batch_id":"260510-oua"' returns 1; line is valid JSON (node -e "JSON.parse(require('fs').readFileSync('data/audit-changes.jsonl','utf8').trim().split('\n').pop())").</automated>
  </verify>
  <done>One new line in data/audit-changes.jsonl with batch_id=260510-oua, op=update, slug=world-capitals, prev_score=45, new_score=70.</done>
</task>

</tasks>

<verification>
- `SELECT score FROM question_categories WHERE question_id=<id> AND category_slug='world-capitals'` returns 70.
- All other rows for that question_id are unchanged in score.
- `data/audit-changes.jsonl` last line contains `"batch_id":"260510-oua"` and `"new_score":70`.
- No files modified under `supabase/migrations/`.
</verification>

<success_criteria>
- Ouagadougou's world-capitals score in Supabase is 70 (was 45).
- Exactly one audit row appended; format matches existing 999.23 entries.
- Zero schema changes, zero code changes.
</success_criteria>

<output>
After completion, create `.planning/quick/260510-j5k-260510-oua-ouagadougou-world-capitals-sc/260510-j5k-01-SUMMARY.md` capturing: question_id, prev/new score, audit timestamp.
</output>
