---
phase: 02.1-question-pipeline-qa-agent-source-relevance
verified: 2026-04-05T15:13:00Z
status: passed
score: 10/10 must-haves verified
---

# Phase 02.1: QA Agent & Source Relevance Verification Report

**Phase Goal:** Add a QA Agent (5th pipeline step) that reviews questions for natural language, category fit, difficulty calibration, distractor quality, and readability -- rewriting where fixable, rejecting when broken. Also improve Knowledge Agent with better Wikipedia search queries, Haiku-powered source relevance filtering, and fallback search terms. Move auto-publish from Fact-Check to QA Agent.
**Verified:** 2026-04-05T15:13:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Fact-Check Agent only sets verified/rejected status, never published | VERIFIED | `fact-check.ts` has zero occurrences of `'published'` or `published_at`. Both branches set `status: 'verified' as const` or `status: 'rejected' as const`. |
| 2 | QA Agent runs 4 quality checks per question | VERIFIED | `qa.ts` SYSTEM_PROMPT names all 4 dimensions. `QaResultSchema` requires `natural_language_score`, `category_fit_score`, `difficulty_calibration_score`, `distractor_quality_score`. |
| 3 | QA Agent rewrites fixable questions, rejects broken ones | VERIFIED | `qa.ts` handles `action === 'rewrite'` (updates DB, sets `qa_rewritten: true`) and `action === 'reject'` (sets status to `'rejected'`). |
| 4 | QA Agent publishes score >= 3 questions that pass QA; leaves score 1-2 as verified | VERIFIED | `qa.ts` lines 211-213 and 239-246: publish branch checks `originalQuestion.verification_score >= 3`. Score < 3 path logs "stays verified" and makes no DB update. |
| 5 | QA Agent keeps existing verification score when rewriting | VERIFIED | `qa.ts` rewrite block updates `question_text`, `distractors`, `explanation`, `qa_rewritten` only -- no `verification_score` field written. Comment on line 195 says "Keep existing verification_score (D-05)". |
| 6 | Knowledge Agent builds search queries from category hierarchy | VERIFIED | `knowledge.ts` fetches `parent_id`, builds `searchTerms = ${category.name} ${parent.name}` for child categories, and appends child names for top-level categories. |
| 7 | Knowledge Agent filters Wikipedia articles via Haiku relevance scoring | VERIFIED | `knowledge.ts` calls `claude.messages.create` with relevance prompt per article, parses JSON score, skips articles below `config.relevanceThreshold` (default 0.6). |
| 8 | Knowledge Agent retries with fallback search when initial query < 2 results | VERIFIED | `knowledge.ts` lines 109-124: `if (titles.length < 2)` block retries with `parentName` or first word of category name, merges and deduplicates results. |
| 9 | Pipeline runs QA as 5th step after Fact-Check | VERIFIED | `run-pipeline.ts` imports `runQaAgent` and calls it sequentially after `runFactCheckAgent`. Pipeline test asserts `callOrder` is `['category', 'knowledge', 'questions', 'fact-check', 'qa']`. |
| 10 | Pipeline success record includes QA stats | VERIFIED | `run-pipeline.ts` lines 118-120: success update writes `questions_qa_passed`, `questions_qa_rewritten`, `questions_qa_rejected` from `qaResult`. Pipeline test asserts all three values. |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/00002_qa_agent.sql` | QA stats columns on pipeline_runs, qa_rewritten on questions | VERIFIED | Contains `questions_qa_passed INTEGER DEFAULT 0`, `questions_qa_rewritten INTEGER DEFAULT 0`, `questions_qa_rejected INTEGER DEFAULT 0`, `qa_rewritten BOOLEAN DEFAULT false`. |
| `pipeline/src/lib/schemas.ts` | QaResultSchema and QaBatchSchema | VERIFIED | Both exported. `QaResultSchema` has all 4 scoring dimensions. `rewritten_distractors: z.array(z.string()).length(3).optional()` enforces exactly-3 constraint. |
| `pipeline/src/lib/config.ts` | relevanceThreshold in PipelineConfig | VERIFIED | Interface field `relevanceThreshold: number` present. `loadConfig()` returns `relevanceThreshold: envNumber('RELEVANCE_THRESHOLD', 0.6)`. |
| `pipeline/src/lib/database.types.ts` | QA fields synced from migration | VERIFIED | `qa_rewritten: boolean` on questions Row; `questions_qa_passed/rewritten/rejected: number` on pipeline_runs Row (with optional variants on Insert/Update). |
| `pipeline/src/agents/fact-check.ts` | No auto-publish, only verified/rejected | VERIFIED | Zero grep hits for `'published'` or `published_at`. Both result paths confirmed in code. 300 lines, substantive implementation. |
| `pipeline/src/agents/knowledge.ts` | Hierarchy queries, Haiku filtering, fallback search | VERIFIED | All three enhancements present. `tokenAccumulator: TokenAccumulator` in function signature. `BudgetExceededError` re-thrown in both catch blocks. |
| `pipeline/src/agents/qa.ts` | QA Agent with 4 checks, rewrite, publish | VERIFIED | 300 lines. Exports `runQaAgent` and `QaAgentResult`. Handles all three actions (pass/rewrite/reject). `QaBatchSchema.parse` used for response validation. |
| `pipeline/tests/agents/qa.test.ts` | QA Agent test suite | VERIFIED | 12 test cases. Covers all QA flows per plan requirements. |
| `pipeline/src/run-pipeline.ts` | Pipeline with QA as 5th step | VERIFIED | Imports `runQaAgent`, calls it after fact-check, includes QA stats in success record. |
| `pipeline/tests/run-pipeline.test.ts` | Pipeline tests covering QA | VERIFIED | 11 tests. Asserts call order including `'qa'`, QA stats in success record, QA failure handling. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `run-pipeline.ts` | `pipeline/src/agents/qa.ts` | `import { runQaAgent }` + sequential call after fact-check | WIRED | Import on line 10, call on line 97 after fact-check block. |
| `pipeline/src/agents/qa.ts` | `pipeline/src/lib/schemas.ts` | `QaBatchSchema.parse` | WIRED | Import on line 4, `QaBatchSchema.parse(parsed)` on line 144. |
| `pipeline/src/agents/qa.ts` | Supabase questions table | status update to `'published'` | WIRED | Lines 241-245 update `status: 'published' as const` and `published_at` for score >= 3 pass/rewrite paths. |
| `pipeline/src/agents/knowledge.ts` | `pipeline/src/lib/claude.ts` | `createClaudeClient + trackUsage + checkBudget` | WIRED | All imported line 4-5, `trackUsage(relevanceResponse, tokenAccumulator, HAIKU_INPUT, HAIKU_OUTPUT)` called per article. |
| `pipeline/src/agents/knowledge.ts` | `pipeline/src/lib/wikipedia.ts` | `searchArticles` with enriched query | WIRED | `searchArticles(searchTerms, ...)` called line 105 with hierarchy-enriched terms. |
| `pipeline/src/lib/database.types.ts` | `supabase/migrations/00002_qa_agent.sql` | manual type sync | WIRED | All 4 migration columns (`qa_rewritten`, `questions_qa_passed/rewritten/rejected`) present in Row, Insert, and Update types. |
| `pipeline/tests/agents/fact-check.test.ts` | `pipeline/src/agents/fact-check.ts` | vitest assertions | WIRED | Tests assert `status: 'verified'` (not `'published'`), no `published_at` assertions. 13 tests pass. |

### Data-Flow Trace (Level 4)

Not applicable. All artifacts are pipeline agents and library modules -- no React components or dynamic render paths to trace. Data flows verified via test coverage (93 tests passing).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite passes | `cd pipeline && npx vitest run` | 93 tests pass across 10 test files, 0 failures | PASS |
| TypeScript compiles clean | `./node_modules/.bin/tsc --noEmit` | No output (exit 0) | PASS |
| No auto-publish in fact-check | `grep 'published' pipeline/src/agents/fact-check.ts` | No output | PASS |
| QA agent exports runQaAgent | File read + grep | `export async function runQaAgent(` present on line 33 | PASS |
| QaBatchSchema imported in qa.ts | File read | Line 4: `import { QaBatchSchema } from '../lib/schemas.js'` | PASS |
| Pipeline calls 5 agents in order | `grep -n "runQaAgent\|runFactCheck\|runQuestions\|runKnowledge\|runCategory" run-pipeline.ts` | All 5 imports and calls present, QA last | PASS |

### Requirements Coverage

D-series requirements are defined in `02.1-CONTEXT.md` (phase-level decisions), not the global `REQUIREMENTS.md`. The global requirements file uses PIPE-xx/DB-xx/etc identifiers and does not contain D-01 through D-10. All D-series IDs are phase-internal decision references.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| D-01 | Plan 03 | QA Agent runs 4 quality checks | SATISFIED | `SYSTEM_PROMPT` in `qa.ts` defines all 4. `QaResultSchema` enforces all 4 score fields. |
| D-02 | Plan 03 | QA rewrites fixable, rejects broken | SATISFIED | `rewrite` and `reject` action branches fully implemented in `qa.ts`. |
| D-03 | Plan 01 | Auto-publish moves from Fact-Check to QA Agent | SATISFIED | `fact-check.ts` has zero `'published'` references. `qa.ts` handles publish for score >= 3. |
| D-04 | Plan 03 | QA reviews all verified questions (score >= 1); score < 3 stays verified | SATISFIED | `qa.ts` queries `.gte('verification_score', 1)`. Score < 3 pass path leaves status unchanged. |
| D-05 | Plan 01 | QA rewrites keep existing verification score | SATISFIED | `qa.ts` rewrite block never writes `verification_score` field. Comment says "Keep existing verification_score (D-05)". |
| D-06 | Plan 02 | Knowledge Agent uses Haiku to score source relevance | SATISFIED | `knowledge.ts` calls `claude.messages.create` per article, filters below `config.relevanceThreshold`. |
| D-07 | Plan 02 | Knowledge Agent builds richer search queries from category hierarchy | SATISFIED | `knowledge.ts` fetches parent and child categories, appends names to `searchTerms`. |
| D-08 | Plan 02 | Knowledge Agent retries with fallback when < 2 results | SATISFIED | `knowledge.ts` `if (titles.length < 2)` block retries once with broader terms. |
| D-09 | Plan 03 | Pipeline sequence is Category -> Knowledge -> Questions -> Fact-Check -> QA | SATISFIED | `run-pipeline.ts` calls all 5 agents in that order. Pipeline test asserts `callOrder`. |
| D-10 | Plan 03 | QA Agent processes all verified questions (score >= 1) | SATISFIED | Same as D-04 evidence -- `.gte('verification_score', 1)` filter confirmed. |

**Global REQUIREMENTS.md coverage:** The D-series IDs used in the plan frontmatter are phase-internal decision labels, not entries in `REQUIREMENTS.md`. The global REQUIREMENTS.md tracks app-level requirements (PIPE-xx, DB-xx, etc). No orphaned requirements found -- D-01 through D-10 are all accounted for in plans 01-03.

### Anti-Patterns Found

None found.

Checked all phase key files for TODO/FIXME/placeholder comments, empty implementations, hardcoded empty data, and stub patterns:

- `pipeline/src/agents/qa.ts` -- 300 lines, full implementation, no stubs
- `pipeline/src/agents/knowledge.ts` -- full implementation with three enhancements
- `pipeline/src/agents/fact-check.ts` -- simplified correctly, no dead code
- `pipeline/src/run-pipeline.ts` -- 5-agent wiring complete
- `supabase/migrations/00002_qa_agent.sql` -- correct DDL, no placeholders
- `pipeline/src/lib/schemas.ts` -- both QA schemas fully defined
- `pipeline/src/lib/config.ts` -- `relevanceThreshold` wired correctly

### Human Verification Required

None. All phase goals are verifiable through code inspection and automated tests. There are no UI components, visual behaviors, or external service dependencies requiring manual testing in this phase.

### Gaps Summary

No gaps. All 10 must-have truths verified. All artifacts exist, are substantive, and are correctly wired. The full test suite of 93 tests passes with zero failures. TypeScript compiles without errors.

---

_Verified: 2026-04-05T15:13:00Z_
_Verifier: Claude (gsd-verifier)_
