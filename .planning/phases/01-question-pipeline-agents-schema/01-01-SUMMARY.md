---
phase: 01-question-pipeline-agents-schema
plan: 01
subsystem: database, pipeline
tags: [supabase, postgresql, rls, typescript, anthropic-sdk, zod, wikipedia, vitest, eslint9]

requires:
  - phase: none
    provides: greenfield project
provides:
  - Supabase PostgreSQL schema with categories, sources, questions, pipeline_runs tables
  - RLS policies for public read on categories and published questions
  - 12 seed categories as root-level entries
  - Pipeline TypeScript project with all shared libraries
  - Typed Supabase client, Claude client with budget tracking, Wikipedia API helper
  - Zod schemas for all agent structured outputs
  - Structured logger, config loader, manual Database types
affects: [01-02, 01-03, 01-04]

tech-stack:
  added: ["@anthropic-ai/sdk", "@supabase/supabase-js", "zod", "date-fns", "dotenv", "tsx", "typescript", "vitest", "eslint", "prettier", "typescript-eslint"]
  patterns: ["ESLint 9 flat config", "NodeNext module resolution", "TypeScript strict mode", "structured logging with timestamp+level", "env-based config with required/optional defaults"]

key-files:
  created:
    - supabase/migrations/00001_initial_schema.sql
    - supabase/seed.sql
    - supabase/config.toml
    - pipeline/package.json
    - pipeline/package-lock.json
    - pipeline/tsconfig.json
    - pipeline/.env.example
    - pipeline/.gitignore
    - pipeline/eslint.config.js
    - pipeline/vitest.config.ts
    - pipeline/src/lib/supabase.ts
    - pipeline/src/lib/claude.ts
    - pipeline/src/lib/wikipedia.ts
    - pipeline/src/lib/schemas.ts
    - pipeline/src/lib/config.ts
    - pipeline/src/lib/logger.ts
    - pipeline/src/lib/database.types.ts
    - pipeline/README.md
  modified: []

key-decisions:
  - "Manual Database types as temporary bridge until supabase gen types is run against linked project"
  - "ESLint 9 flat config with typescript-eslint (not legacy .eslintrc.json)"
  - "MediaWiki Action API for Wikipedia content extraction per D-09"

patterns-established:
  - "Shared library pattern: all agent dependencies in pipeline/src/lib/"
  - "Config pattern: required env vars throw, optional env vars have defaults"
  - "Budget tracking: TokenAccumulator with per-model cost constants"
  - "Structured logging: log(level, message, data?) with ISO timestamp prefix"

requirements-completed: [DB-01, DB-02, PIPE-01, PIPE-08, COST-03]

duration: 3min
completed: 2026-04-04
---

# Phase 01 Plan 01: Schema & Shared Libraries Summary

**Supabase PostgreSQL schema with 4 tables, RLS policies, 12 seed categories, and pipeline TypeScript project with typed clients for Supabase, Claude (with budget tracking), and Wikipedia**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-04T18:49:09Z
- **Completed:** 2026-04-04T18:52:35Z
- **Tasks:** 2
- **Files modified:** 19

## Accomplishments
- Complete database schema with categories (adjacency list, depth 0-3), sources, questions (with distractors JSONB, verification score), and pipeline_runs tables
- RLS enabled on all 4 tables with public read for categories and published questions; pipeline uses service-role key bypassing RLS
- 12 seed categories covering Science, History, Geography, Movies and TV, Music, Gaming, Sports, Food and Drink, Literature, Art and Design, Technology, Nature and Animals
- Pipeline TypeScript project compiles cleanly with strict mode, ESLint 9 flat config, Vitest, and Prettier configured
- Shared libraries: typed Supabase client, Claude client with token accumulator and budget enforcement, Wikipedia MediaWiki Action API helper, Zod schemas for all agent outputs, structured logger, config loader

## Task Commits

Each task was committed atomically:

1. **Task 1: Create Supabase schema migration, RLS policies, seed data, and initialize Supabase CLI** - `1d174cb` (feat)
2. **Task 2: Create pipeline project with shared libraries, .gitignore, lockfile, and ESLint flat config** - `c825fbb` (feat)
3. **Supabase .gitignore from CLI init** - `b6f9506` (chore)

## Files Created/Modified
- `supabase/migrations/00001_initial_schema.sql` - 4 tables with indexes, constraints, RLS policies
- `supabase/seed.sql` - 12 root-level seed categories
- `supabase/config.toml` - Supabase CLI configuration (generated by `supabase init`)
- `pipeline/package.json` - Pipeline project with all dependencies
- `pipeline/package-lock.json` - Locked dependency tree for `npm ci` in CI
- `pipeline/tsconfig.json` - TypeScript strict mode, ES2022, NodeNext
- `pipeline/.env.example` - Environment variable template
- `pipeline/.gitignore` - Excludes node_modules, dist, .env, tsbuildinfo
- `pipeline/eslint.config.js` - ESLint 9 flat config with typescript-eslint
- `pipeline/vitest.config.ts` - Vitest configuration with globals
- `pipeline/src/lib/supabase.ts` - Typed Supabase service-role client factory
- `pipeline/src/lib/claude.ts` - Anthropic client with token tracking and budget enforcement
- `pipeline/src/lib/wikipedia.ts` - MediaWiki Action API helper (getArticleText, searchArticles)
- `pipeline/src/lib/schemas.ts` - Zod schemas for Category, Question, FactCheck agent outputs
- `pipeline/src/lib/config.ts` - Config loader with env validation and defaults
- `pipeline/src/lib/logger.ts` - Structured logging with timestamp and level prefix
- `pipeline/src/lib/database.types.ts` - Manual Database type (temporary bridge)
- `pipeline/README.md` - Setup checklist for Supabase project, CLI, migrations, secrets

## Decisions Made
- Manual Database types as temporary bridge until `supabase gen types` is run against a linked project. File structure supports drop-in replacement.
- ESLint 9 flat config chosen over legacy .eslintrc.json since ESLint 9 dropped legacy config support.
- MediaWiki Action API used for Wikipedia content extraction per D-09 (RESTBase being deprecated).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

External services require manual configuration. See `pipeline/README.md` for:
- Creating a Supabase project and linking it
- Applying migrations and seed data
- Setting GitHub Actions secrets (ANTHROPIC_API_KEY, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)
- Copying .env.example to .env for local development

## Next Phase Readiness
- Schema is ready for all 4 agents (Category, Knowledge, Questions, Fact-Check)
- Shared libraries are importable and provide typed clients for Supabase, Claude, and Wikipedia
- Zod schemas define the expected structured output format for each agent
- Budget tracking is built into the Claude client for COST-03 compliance
- Plans 01-02 through 01-04 can build agents using these shared libraries

## Self-Check: PASSED

- All 18 created files verified present
- All 3 commit hashes verified in git log (1d174cb, c825fbb, b6f9506)

---
*Phase: 01-question-pipeline-agents-schema*
*Completed: 2026-04-04*
