---
phase: 01-question-pipeline-agents-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00001_initial_schema.sql
  - supabase/seed.sql
  - supabase/config.toml
  - pipeline/package.json
  - pipeline/package-lock.json
  - pipeline/tsconfig.json
  - pipeline/.env.example
  - pipeline/.gitignore
  - pipeline/eslint.config.js
  - pipeline/src/lib/supabase.ts
  - pipeline/src/lib/claude.ts
  - pipeline/src/lib/wikipedia.ts
  - pipeline/src/lib/schemas.ts
  - pipeline/src/lib/config.ts
  - pipeline/src/lib/logger.ts
  - pipeline/src/lib/database.types.ts
  - pipeline/vitest.config.ts
  - pipeline/README.md
autonomous: true
requirements:
  - DB-01
  - DB-02
  - PIPE-01
  - PIPE-08
  - COST-03

must_haves:
  truths:
    - "Supabase PostgreSQL schema exists with tables for categories, sources, questions, and pipeline_runs"
    - "RLS is enabled on all tables with public read for categories and published questions, no public write"
    - "12 seed categories exist in the categories table"
    - "Pipeline project can install dependencies and run TypeScript files"
    - "Shared libraries provide typed Supabase client, Claude client with token tracking, and Wikipedia fetcher"
    - "Supabase CLI is initialized and migrations can be applied"
    - "Pipeline has a .gitignore preventing node_modules, dist, and .env from being committed"
  artifacts:
    - path: "supabase/migrations/00001_initial_schema.sql"
      provides: "All table definitions, indexes, RLS policies, CHECK constraints"
      contains: "CREATE TABLE categories"
    - path: "supabase/seed.sql"
      provides: "12 seed categories inserted as root-level entries"
      contains: "INSERT INTO categories"
    - path: "pipeline/package.json"
      provides: "Pipeline project with all dependencies"
      contains: "@anthropic-ai/sdk"
    - path: "pipeline/package-lock.json"
      provides: "Locked dependency tree for reproducible installs (required by npm ci in CI)"
    - path: "pipeline/.gitignore"
      provides: "Git ignore rules for node_modules, dist, .env, tsbuildinfo"
    - path: "pipeline/eslint.config.js"
      provides: "ESLint 9 flat config with TypeScript support"
    - path: "pipeline/src/lib/supabase.ts"
      provides: "Typed Supabase service-role client"
      exports: ["supabase"]
    - path: "pipeline/src/lib/claude.ts"
      provides: "Anthropic client with token tracking and budget enforcement"
      exports: ["createClaudeClient", "trackUsage", "checkBudget"]
    - path: "pipeline/src/lib/wikipedia.ts"
      provides: "MediaWiki Action API helper for article text extraction"
      exports: ["getArticleText", "searchArticles"]
    - path: "pipeline/src/lib/schemas.ts"
      provides: "Zod schemas for all agent outputs"
      exports: ["CategorySchema", "QuestionSchema", "FactCheckSchema"]
    - path: "pipeline/src/lib/logger.ts"
      provides: "Structured logging helper with timestamp and level prefix"
      exports: ["log"]
    - path: "pipeline/src/lib/database.types.ts"
      provides: "Manual Database type (temporary bridge until supabase gen types is run against linked project)"
    - path: "pipeline/README.md"
      provides: "Setup checklist for Supabase project creation, CLI, linking, migrations, and GitHub secrets"
  key_links:
    - from: "pipeline/src/lib/supabase.ts"
      to: "pipeline/src/lib/database.types.ts"
      via: "Imports Database type for typed client"
      pattern: "Database"
    - from: "pipeline/src/lib/claude.ts"
      to: "pipeline/src/lib/config.ts"
      via: "Budget cap from config"
      pattern: "budgetCapUsd"
---

<objective>
Create the Supabase database schema (tables, indexes, RLS, seed data), initialize the Supabase CLI project, and create the pipeline TypeScript project with all shared libraries that the 4 agents will use.

Purpose: This is the foundation for everything in Phase 1. The schema defines where data lives. The shared libraries (Claude client, Supabase client, Wikipedia helper, Zod schemas, config, logger) are imported by every agent script. Without this, no agent can be built.

Output: A working Supabase migration (with CLI initialized), 12 seed categories, and a pipeline/ project with installable dependencies, lockfile, and tested shared libraries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-question-pipeline-agents-schema/01-CONTEXT.md
@.planning/phases/01-question-pipeline-agents-schema/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase schema migration, RLS policies, seed data, and initialize Supabase CLI</name>
  <files>supabase/migrations/00001_initial_schema.sql, supabase/seed.sql, supabase/config.toml, pipeline/README.md</files>
  <read_first>
    .planning/phases/01-question-pipeline-agents-schema/01-RESEARCH.md (contains full schema SQL starting at line 424)
    .planning/phases/01-question-pipeline-agents-schema/01-CONTEXT.md (decisions D-12 through D-15)
  </read_first>
  <action>
Create the Supabase project structure, initial migration, and setup documentation.

**Supabase CLI initialization**: Run `npx supabase init` in the project root to create the `supabase/` directory structure. This generates `supabase/config.toml` automatically. If `supabase/` already exists, the command will use it. The init command sets up the local development structure -- linking to a remote project and applying migrations requires manual setup (documented in README below).

**supabase/migrations/00001_initial_schema.sql**: Create 4 tables with these exact specifications:

1. `categories` table (per D-12, adjacency list):
   - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
   - `name TEXT NOT NULL`
   - `slug TEXT NOT NULL UNIQUE`
   - `parent_id UUID REFERENCES categories(id) ON DELETE SET NULL`
   - `depth INTEGER NOT NULL DEFAULT 0 CHECK (depth >= 0 AND depth <= 3)`
   - `description TEXT`
   - `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`
   - `created_by TEXT NOT NULL DEFAULT 'pipeline'`
   - Indexes: `idx_categories_parent` on parent_id, `idx_categories_slug` on slug

2. `sources` table (per D-10):
   - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
   - `category_id UUID NOT NULL REFERENCES categories(id) ON DELETE CASCADE`
   - `title TEXT NOT NULL` (Wikipedia article title)
   - `url TEXT NOT NULL` (full Wikipedia URL)
   - `content TEXT NOT NULL` (plain text extract)
   - `content_hash TEXT NOT NULL` (SHA-256 for dedup)
   - `fetched_at TIMESTAMPTZ NOT NULL DEFAULT now()`
   - Indexes: `idx_sources_category` on category_id, `idx_sources_content_hash` UNIQUE on content_hash

3. `questions` table (per D-13, D-14):
   - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
   - `category_id UUID NOT NULL REFERENCES categories(id) ON DELETE CASCADE`
   - `source_id UUID REFERENCES sources(id) ON DELETE SET NULL`
   - `question_text TEXT NOT NULL`
   - `correct_answer TEXT NOT NULL`
   - `distractors JSONB NOT NULL DEFAULT '[]'::jsonb` (per D-13, array of 3 strings)
   - `explanation TEXT`
   - `difficulty TEXT NOT NULL CHECK (difficulty IN ('easy', 'normal', 'hard'))`
   - `verification_score INTEGER NOT NULL DEFAULT 0 CHECK (verification_score >= 0 AND verification_score <= 3)` (per D-14)
   - `status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'verified', 'rejected', 'published'))`
   - `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`
   - `published_at TIMESTAMPTZ`
   - `CONSTRAINT chk_distractors_count CHECK (jsonb_array_length(distractors) = 3)`
   - Indexes: `idx_questions_category`, `idx_questions_status`, `idx_questions_difficulty`, `idx_questions_verification`, `idx_questions_published` (partial, WHERE status = 'published')

4. `pipeline_runs` table (per D-15):
   - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
   - `started_at TIMESTAMPTZ NOT NULL DEFAULT now()`
   - `completed_at TIMESTAMPTZ`
   - `status TEXT NOT NULL DEFAULT 'running' CHECK (status IN ('running', 'success', 'failed'))`
   - `error_message TEXT`
   - Per-agent metrics: `categories_processed INTEGER DEFAULT 0`, `categories_failed INTEGER DEFAULT 0`, `sources_fetched INTEGER DEFAULT 0`, `sources_failed INTEGER DEFAULT 0`, `questions_generated INTEGER DEFAULT 0`, `questions_failed INTEGER DEFAULT 0`, `questions_verified INTEGER DEFAULT 0`, `questions_rejected INTEGER DEFAULT 0`
   - Cost tracking: `total_input_tokens INTEGER DEFAULT 0`, `total_output_tokens INTEGER DEFAULT 0`, `estimated_cost_usd NUMERIC(10, 4) DEFAULT 0`
   - Config snapshot: `config JSONB`
   - Indexes: `idx_pipeline_runs_status`, `idx_pipeline_runs_started` (DESC)

**RLS policies** (per DB-02):
- Enable RLS on ALL 4 tables
- `categories`: SELECT policy with `USING (true)` -- public read
- `questions`: SELECT policy with `USING (status = 'published')` -- only published questions readable
- `sources`: No public policies (internal, pipeline-only via service-role)
- `pipeline_runs`: No public policies (internal, pipeline-only via service-role)
- No INSERT/UPDATE/DELETE policies for anon/authenticated -- pipeline uses service-role key which bypasses RLS

**supabase/seed.sql**: Insert the 12 seed categories as root-level entries (depth=0, parent_id=NULL):
Science, History, Geography, Movies and TV, Music, Gaming, Sports, Food and Drink, Literature, Art and Design, Technology, Nature and Animals.
Each with a slugified name (e.g., `movies-and-tv`), `created_by = 'seed'`, and a brief description.

**pipeline/README.md**: Create a minimal setup checklist (not prose -- a numbered checklist):
1. Create a Supabase project at https://supabase.com/dashboard
2. Install the Supabase CLI: `npm install -g supabase` (or `brew install supabase/tap/supabase` on macOS)
3. Link to your remote project: `supabase link --project-ref <your-project-ref>` (find project ref in Supabase Dashboard > Project Settings > General)
4. Apply the migration: `supabase db push`
5. Apply seed data: `supabase db reset` (applies migrations + seed in local dev) or run seed.sql manually against remote
6. Generate TypeScript types (optional, replaces manual types): `npx supabase gen types typescript --linked > pipeline/src/lib/database.types.ts`
7. Set GitHub Actions secrets: `ANTHROPIC_API_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (Dashboard > Project Settings > API for Supabase values)
8. Copy `pipeline/.env.example` to `pipeline/.env` and fill in values for local development
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE" supabase/migrations/00001_initial_schema.sql | grep -q "4" && grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/00001_initial_schema.sql | grep -q "4" && grep -c "CREATE POLICY" supabase/migrations/00001_initial_schema.sql | grep -q "2" && grep -c "INSERT INTO categories" supabase/seed.sql | grep -q "1" && test -f supabase/config.toml && test -f pipeline/README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase init` has been run (supabase/config.toml exists)
    - supabase/migrations/00001_initial_schema.sql contains exactly 4 CREATE TABLE statements (categories, sources, questions, pipeline_runs)
    - supabase/migrations/00001_initial_schema.sql contains "ENABLE ROW LEVEL SECURITY" 4 times
    - supabase/migrations/00001_initial_schema.sql contains "CREATE POLICY" 2 times (categories SELECT, questions SELECT)
    - supabase/migrations/00001_initial_schema.sql contains "chk_distractors_count" CHECK constraint
    - supabase/migrations/00001_initial_schema.sql contains "jsonb_array_length(distractors) = 3"
    - supabase/migrations/00001_initial_schema.sql contains CHECK constraint for depth (0-3)
    - supabase/migrations/00001_initial_schema.sql contains CHECK constraint for verification_score (0-3)
    - supabase/migrations/00001_initial_schema.sql contains CHECK constraint for difficulty IN ('easy', 'normal', 'hard')
    - supabase/migrations/00001_initial_schema.sql contains CHECK constraint for status IN ('pending', 'verified', 'rejected', 'published')
    - supabase/seed.sql contains 12 INSERT statements or a single INSERT with 12 value rows for the seed categories
    - supabase/seed.sql contains slugs: science, history, geography, movies-and-tv, music, gaming, sports, food-and-drink, literature, art-and-design, technology, nature-and-animals
    - supabase/config.toml exists (created by `supabase init`)
    - pipeline/README.md exists with numbered setup steps including `supabase link`, `supabase db push`, and GitHub secrets
  </acceptance_criteria>
  <done>All 4 tables created with correct columns, types, constraints, and indexes. RLS enabled on all tables with 2 public read policies. 12 seed categories ready to insert. Supabase CLI initialized. Setup documentation written.</done>
</task>

<task type="auto">
  <name>Task 2: Create pipeline project with shared libraries, .gitignore, lockfile, and ESLint flat config</name>
  <files>pipeline/package.json, pipeline/package-lock.json, pipeline/tsconfig.json, pipeline/.env.example, pipeline/.gitignore, pipeline/eslint.config.js, pipeline/src/lib/supabase.ts, pipeline/src/lib/claude.ts, pipeline/src/lib/wikipedia.ts, pipeline/src/lib/schemas.ts, pipeline/src/lib/config.ts, pipeline/src/lib/logger.ts, pipeline/src/lib/database.types.ts, pipeline/vitest.config.ts</files>
  <read_first>
    .planning/phases/01-question-pipeline-agents-schema/01-RESEARCH.md (architecture patterns starting at line 110, code examples starting at line 139)
    .planning/phases/01-question-pipeline-agents-schema/01-CONTEXT.md (decisions D-02, D-03, D-04, D-09)
    CLAUDE.md (technology stack, Zod 3.x requirement, ESLint + Prettier requirement)
  </read_first>
  <action>
Create the pipeline TypeScript project and all shared libraries per D-03 (TypeScript) and D-04 (/pipeline directory).

**pipeline/.gitignore**: Create with entries for:
```
node_modules/
dist/
.env
*.tsbuildinfo
```

**pipeline/package.json**: Initialize with:
- name: "pub-quiz-pipeline"
- type: "module"
- scripts: `"pipeline": "tsx src/run-pipeline.ts"`, `"test": "vitest run"`, `"test:watch": "vitest"`, `"lint": "eslint src/ tests/"`, `"format": "prettier --write 'src/**/*.ts' 'tests/**/*.ts'"`
- dependencies: `@anthropic-ai/sdk@^0.82.0`, `@supabase/supabase-js@^2.101.1`, `zod@^3.24.0`, `date-fns@^4.0.0`, `dotenv@^16.0.0`
- devDependencies: `tsx@^4.21.0`, `typescript@^5.0.0`, `@types/node@^22.0.0`, `vitest@^3.0.0`, `eslint@^9.0.0`, `prettier@^3.0.0`, `typescript-eslint@^8.0.0`

After creating package.json, run `cd pipeline && npm install` to generate `pipeline/package-lock.json`. This lockfile MUST be committed -- the GitHub Actions workflow uses `npm ci` which requires it.

**pipeline/eslint.config.js**: ESLint 9 flat config (NOT .eslintrc.json -- ESLint 9 dropped legacy config support):
```javascript
import tseslint from 'typescript-eslint';

export default tseslint.config(
  ...tseslint.configs.recommended,
  {
    rules: {
      '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
    },
  },
  {
    ignores: ['dist/', 'node_modules/'],
  }
);
```

**pipeline/tsconfig.json**: TypeScript strict mode config:
- target: ES2022, module: NodeNext, moduleResolution: NodeNext
- strict: true, esModuleInterop: true, skipLibCheck: true
- outDir: dist, rootDir: src
- include: ["src/**/*.ts"], exclude: ["node_modules", "dist"]

**pipeline/.env.example**:
```
ANTHROPIC_API_KEY=sk-ant-...
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
PIPELINE_BUDGET_USD=1.00
CATEGORY_BATCH_SIZE=5
KNOWLEDGE_BATCH_SIZE=10
QUESTIONS_BATCH_SIZE=20
```

**pipeline/src/lib/logger.ts**: A thin structured logging helper (no dependencies -- just a wrapper over console). This makes debugging GitHub Actions failures much easier than raw `console.log`:
```typescript
type LogLevel = 'info' | 'warn' | 'error' | 'debug';

export function log(level: LogLevel, message: string, data?: Record<string, unknown>): void {
  const timestamp = new Date().toISOString();
  const prefix = `[${timestamp}] [${level.toUpperCase()}]`;
  const suffix = data ? ` ${JSON.stringify(data)}` : '';
  const output = `${prefix} ${message}${suffix}`;

  if (level === 'error') {
    console.error(output);
  } else if (level === 'warn') {
    console.warn(output);
  } else {
    console.log(output);
  }
}
```
Export the `log` function and `LogLevel` type. All agents and the orchestrator should use `log()` instead of raw `console.log`/`console.error`.

**pipeline/src/lib/config.ts**: Export a `loadConfig()` function that reads from environment variables with defaults:
- `anthropicApiKey`: from `ANTHROPIC_API_KEY` (required, throw if missing)
- `supabaseUrl`: from `SUPABASE_URL` (required, throw if missing)
- `supabaseServiceRoleKey`: from `SUPABASE_SERVICE_ROLE_KEY` (required, throw if missing)
- `budgetCapUsd`: from `PIPELINE_BUDGET_USD`, default `1.00`
- `categoryBatchSize`: from `CATEGORY_BATCH_SIZE`, default `5`
- `knowledgeBatchSize`: from `KNOWLEDGE_BATCH_SIZE`, default `10`
- `questionsBatchSize`: from `QUESTIONS_BATCH_SIZE`, default `20`
- `claudeModelGeneration`: default `'claude-sonnet-4-5-20250514'` (for Category + Questions agents)
- `claudeModelVerification`: default `'claude-haiku-4-5-20250514'` (for Fact-Check agent)
- `wikipediaUserAgent`: default `'PubQuizPipeline/1.0 (https://github.com/pub-quiz)'`
- `wikipediaMaxContentLength`: default `3000` (characters, per research recommendation)
Export the `PipelineConfig` type.

**pipeline/src/lib/database.types.ts**: Define a TEMPORARY manual `Database` type that mirrors the schema -- tables for categories, sources, questions, pipeline_runs with their column types. Add a prominent comment at the top:
```typescript
// TEMPORARY: Manual Database type. Replace with generated types by running:
//   npx supabase gen types typescript --linked > src/lib/database.types.ts
// This manual type is a bridge until the Supabase project is linked.
// The file structure supports drop-in replacement with generated types.
```
This file is imported by supabase.ts. The path and export name (`Database`) are chosen so that running `supabase gen types` produces a drop-in replacement.

**pipeline/src/lib/supabase.ts** (per Pattern 3 from research):
- Import `createClient` from `@supabase/supabase-js`
- Export `createSupabaseClient(url: string, serviceRoleKey: string)` that creates a typed client using `Database` generic
- Import the `Database` type from `./database.types.ts` (NOT inline -- separate file for easy replacement with generated types)

**pipeline/src/lib/claude.ts** (per Pattern 2 from research):
- Import `Anthropic` from `@anthropic-ai/sdk`
- Export `createClaudeClient(apiKey: string)` returning an Anthropic instance
- Export `TokenAccumulator` interface: `{ input_tokens: number, output_tokens: number, estimated_cost_usd: number }`
- Export `createTokenAccumulator()` returning a fresh accumulator
- Export `trackUsage(response: Anthropic.Message, accumulator: TokenAccumulator, inputCostPerMTok: number, outputCostPerMTok: number): void` that adds response.usage to the accumulator and calculates cost
- Export `checkBudget(accumulator: TokenAccumulator, budgetCapUsd: number): void` that throws `BudgetExceededError` if exceeded
- Export `BudgetExceededError` class extending Error
- Cost constants: `SONNET_INPUT = 3`, `SONNET_OUTPUT = 15`, `HAIKU_INPUT = 1`, `HAIKU_OUTPUT = 5` (dollars per million tokens)

**pipeline/src/lib/wikipedia.ts** (per Pattern 4 from research, using MediaWiki Action API per D-09):
- Base URL: `https://en.wikipedia.org/w/api.php`
- Export `getArticleText(title: string, userAgent: string, maxLength?: number): Promise<string | null>` -- uses `action=query&prop=extracts&explaintext=1&titles={title}` params. Truncates content to `maxLength` characters (default 3000). Returns null if page is missing.
- Export `searchArticles(query: string, userAgent: string, limit?: number): Promise<string[]>` -- uses `action=query&list=search&srsearch={query}` params. Returns array of article titles.
- Both functions set `formatversion=2` and `format=json` params.
- Both functions include `User-Agent` header with the provided userAgent string.
- Note in code comment: "Using MediaWiki Action API per D-09. Action API provides full article text extraction and is the long-term stable choice."

**pipeline/src/lib/schemas.ts**: Zod 3.x schemas for all agent structured outputs:
- `CategoryProposalSchema`: `z.object({ name: z.string(), slug: z.string(), description: z.string(), parent_slug: z.string() })`
- `CategoryBatchSchema`: `z.object({ categories: z.array(CategoryProposalSchema) })`
- `QuestionGeneratedSchema`: `z.object({ question_text: z.string(), correct_answer: z.string(), distractors: z.array(z.string()).length(3), explanation: z.string(), difficulty: z.enum(['easy', 'normal', 'hard']) })`
- `QuestionBatchSchema`: `z.object({ questions: z.array(QuestionGeneratedSchema) })`
- `FactCheckResultSchema`: `z.object({ question_id: z.string().uuid(), is_correct: z.boolean(), verification_score: z.number().int().min(0).max(3), reasoning: z.string() })`
- `FactCheckBatchSchema`: `z.object({ results: z.array(FactCheckResultSchema) })`
- Export all schemas and their inferred types (e.g., `type CategoryProposal = z.infer<typeof CategoryProposalSchema>`)

**pipeline/vitest.config.ts**: Minimal Vitest config:
```typescript
import { defineConfig } from 'vitest/config';
export default defineConfig({ test: { globals: true } });
```
  </action>
  <verify>
    <automated>cd /Users/jamiepersonal/Developer/pub-quiz/pipeline && test -f package-lock.json && test -f .gitignore && test -f eslint.config.js && test -f src/lib/logger.ts && test -f src/lib/database.types.ts && npx tsc --noEmit && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/package.json contains "@anthropic-ai/sdk", "@supabase/supabase-js", "zod", "date-fns", "dotenv", "tsx", "typescript", "vitest"
    - pipeline/package.json contains "eslint", "prettier", "typescript-eslint" in devDependencies
    - pipeline/package.json contains "lint" and "format" scripts
    - pipeline/package-lock.json exists (generated by `npm install`, committed to git)
    - pipeline/.gitignore contains node_modules/, dist/, .env, *.tsbuildinfo
    - pipeline/eslint.config.js exists using ESLint 9 flat config format with typescript-eslint (NOT .eslintrc.json)
    - pipeline/tsconfig.json contains "strict": true
    - pipeline/.env.example contains ANTHROPIC_API_KEY, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, PIPELINE_BUDGET_USD
    - pipeline/src/lib/logger.ts exports a `log` function with timestamp + level prefix
    - pipeline/src/lib/database.types.ts contains TEMPORARY comment and manual Database type matching schema
    - pipeline/src/lib/database.types.ts structure supports drop-in replacement with `supabase gen types` output
    - pipeline/src/lib/config.ts exports loadConfig function and PipelineConfig type
    - pipeline/src/lib/config.ts contains default values: budgetCapUsd 1.00, categoryBatchSize 5, knowledgeBatchSize 10, questionsBatchSize 20
    - pipeline/src/lib/supabase.ts imports Database from ./database.types.ts and exports createSupabaseClient function
    - pipeline/src/lib/claude.ts exports createClaudeClient, trackUsage, checkBudget, BudgetExceededError, createTokenAccumulator
    - pipeline/src/lib/claude.ts contains cost constants SONNET_INPUT = 3, HAIKU_INPUT = 1
    - pipeline/src/lib/wikipedia.ts exports getArticleText and searchArticles functions
    - pipeline/src/lib/wikipedia.ts contains "en.wikipedia.org/w/api.php" (Action API per D-09)
    - pipeline/src/lib/schemas.ts exports CategoryProposalSchema, QuestionGeneratedSchema, FactCheckResultSchema
    - pipeline/src/lib/schemas.ts contains ".length(3)" for distractors validation
    - `cd pipeline && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Pipeline project installs cleanly with lockfile generated. TypeScript compiles with no errors. ESLint 9 flat config and Prettier are configured. .gitignore prevents node_modules/dist/.env commits. Structured logger available. Manual Database types in place with clear path to generated types. All shared libraries export the expected functions/types.</done>
</task>

</tasks>

<verification>
- `supabase/migrations/00001_initial_schema.sql` has 4 CREATE TABLE, 4 ENABLE ROW LEVEL SECURITY, 2 CREATE POLICY
- `supabase/seed.sql` has 12 categories
- `supabase/config.toml` exists (Supabase CLI initialized)
- `pipeline/package-lock.json` exists (lockfile generated)
- `pipeline/.gitignore` exists with node_modules/, dist/, .env
- `pipeline/eslint.config.js` exists (flat config, not .eslintrc.json)
- `cd pipeline && npm install && npx tsc --noEmit` passes
- All shared library files exist and export expected symbols
- `pipeline/README.md` contains setup checklist
- `pipeline/src/lib/logger.ts` exports `log` function
- `pipeline/src/lib/database.types.ts` contains TEMPORARY comment
</verification>

<success_criteria>
- Database schema is complete with all tables, indexes, constraints, and RLS policies as specified
- Supabase CLI is initialized (`supabase init` run, config.toml exists)
- Setup documentation in pipeline/README.md covers project creation, linking, migration, and secrets
- Pipeline project compiles cleanly with TypeScript strict mode
- ESLint 9 flat config and Prettier are configured with TypeScript support
- .gitignore prevents accidental commits of node_modules, dist, .env, tsbuildinfo
- package-lock.json is generated and committed for reproducible CI installs
- Structured logger is available for all agents and orchestrator
- Manual Database type is clearly marked as temporary with instructions to generate real types
- Shared libraries are importable and provide typed Supabase client, Claude client with budget tracking, Wikipedia API helper, Zod schemas, and configuration
- Seed data covers all 12 root categories
</success_criteria>

<output>
After completion, create `.planning/phases/01-question-pipeline-agents-schema/01-01-SUMMARY.md`
</output>
