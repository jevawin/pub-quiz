---
phase: 02.2
slug: web-quiz-v1-feedback-collection-inserted
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-07
---

# Phase 02.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (web app), pgTAP/psql-based integration for RLS |
| **Config file** | `apps/web/vitest.config.ts` (Wave 0 installs) |
| **Quick run command** | `cd apps/web && npm test -- --run` |
| **Full suite command** | `cd apps/web && npm test -- --run && npm run test:rls` |
| **Estimated runtime** | ~20 seconds |

---

## Sampling Rate

- **After every task commit:** Run quick command
- **After every plan wave:** Run full suite
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

*Populated by planner after PLAN.md creation.*

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | TBD | TBD | TBD | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `apps/web/` Vite + React + TS scaffold (`npm create vite`)
- [ ] `apps/web/vitest.config.ts` + jsdom + @testing-library/react
- [ ] `apps/web/src/test/setup.ts` — shared fixtures (localStorage reset, fake timers)
- [ ] `supabase/` local dev running (`supabase start`) for RLS integration tests
- [ ] `apps/web/package.json` scripts: `test`, `test:rls`, `build`, `dev`
- [ ] SPA fallback file `apps/web/public/_redirects`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cloudflare Pages deploy reachable on *.pages.dev | Hosting | Requires real deploy | Push branch, check Wrangler Pages deploy URL loads landing page |
| Active-tab timer on real mobile Safari | Timer correctness | Visibility/focus semantics vary on iOS | Play a quiz on iPhone Safari, background app mid-question, confirm timer pauses |
| Share link round-trip across browsers | Share UX | Copy-to-clipboard permissions vary | Copy link, open in another browser, confirm setup pre-filled |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
