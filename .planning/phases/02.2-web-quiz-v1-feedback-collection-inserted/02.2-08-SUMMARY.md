---
phase: 02.2-web-quiz-v1-feedback-collection-inserted
plan: 08
subsystem: testing
tags: [vitest, rls, integration, e2e, supabase]

requires:
  - phase: 02.2-02
    provides: question_plays + quiz_sessions tables, RLS policies, RPC
  - phase: 02.2-05
    provides: Setup screen
  - phase: 02.2-06
    provides: Play screen, recordQuestionPlay
  - phase: 02.2-07
    provides: End screen, recordQuizSession
provides:
  - RLS integration test suite (4 cases, env-guarded)
  - Happy-path E2E test (setup → play → done)
  - test:rls npm script
affects: []

tech-stack:
  added: []
  patterns: [env-guarded integration tests with describe.skip, full-flow jsdom E2E with mocked backend]

key-files:
  created:
    - apps/web/src/lib/rls.integration.test.ts
    - apps/web/src/e2e/happyPath.test.tsx
  modified:
    - apps/web/package.json

key-decisions:
  - "RLS tests skip when TEST_SUPABASE_URL/TEST_SUPABASE_ANON_KEY unset — CI stays green without local DB"
  - "E2E test mocks supabase, auth, questions, plays — tests the UI flow, not the backend"

patterns-established:
  - "Env-guarded integration: describeIntegration = url && anon ? describe : describe.skip"

requirements-completed: [WEB-02, WEB-03, WEB-04, WEB-11]

duration: 3min
completed: 2026-04-11
---

# Plan 08: RLS & E2E Tests Summary

**RLS integration tests prove insert-only policies work against real Postgres, and the happy-path E2E test walks the full setup → play → done flow with 5 questions and feedback submission.**

## What Was Built

1. **RLS integration test** — 4 cases: insert with matching session_id succeeds, mismatched session_id denied, SELECT denied (empty result), RPC returns correct rows. Guarded by env vars, skips cleanly in CI.

2. **Happy-path E2E test** — Full jsdom flow: renders Setup, clicks Play (mocked fetch returns 5 questions), answers all 5 correctly via option buttons + Next feedback, lands on End screen, selects Good rating, types feedback, submits. Asserts recordQuestionPlay called 5 times and recordQuizSession called once with correct shape.

3. **test:rls script** — Added to package.json for targeted integration test runs.

## Self-Check: PASSED
- Both test files exist and pass
- RLS test skips cleanly without env vars (suite stays green)
- 69 tests pass, 4 skipped
