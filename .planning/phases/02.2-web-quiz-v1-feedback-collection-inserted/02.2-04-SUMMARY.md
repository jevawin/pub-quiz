---
phase: 02.2-web-quiz-v1-feedback-collection-inserted
plan: 04
subsystem: state
tags: [timer, outbox, reducer, localStorage, performance.now, vitest, tdd]

requires:
  - phase: 02.2-01
    provides: Vite + vitest scaffold (scaffolded inline as deviation)
provides:
  - createActiveTimer factory (visibility/focus-aware monotonic timer)
  - localStorage outbox with FIFO queue, cap, and flush
  - Fisher-Yates shuffle utility
  - quizReducer state machine (idle -> loading -> playing -> revealed -> finished)
  - selectScore selector
affects: [02.2-05, 02.2-06, 02.2-07]

tech-stack:
  added: []
  patterns: [active-tab timer with visibility/focus gating, localStorage outbox with single-flight flush, discriminated union reducer]

key-files:
  created:
    - apps/web/src/lib/activeTimer.ts
    - apps/web/src/lib/activeTimer.test.ts
    - apps/web/src/lib/outbox.ts
    - apps/web/src/lib/outbox.test.ts
    - apps/web/src/lib/shuffle.ts
    - apps/web/src/lib/shuffle.test.ts
    - apps/web/src/state/quiz.ts
    - apps/web/src/state/quiz.test.ts
  modified: []

key-decisions:
  - "Used performance.now() for monotonic timing, not Date.now()"
  - "Outbox keys versioned with :v1 suffix for future migration"
  - "Quiz reducer uses discriminated union types for type-safe phase transitions"

patterns-established:
  - "Active-tab timer: pause on blur/hidden, resume only when BOTH visible AND focused"
  - "Outbox: FIFO with cap, drop oldest on overflow, single in-flight flush flag"
  - "State machine: pure reducer with no React imports, ready for Phase 7 lift"

requirements-completed: [WEB-05, WEB-06, WEB-07, WEB-08]

duration: 4min
completed: 2026-04-11
---

# Phase 02.2 Plan 04: Timer, Outbox, Reducer Summary

**Pure modules for quiz play: active-tab timer (performance.now + visibility/focus), localStorage FIFO outbox (capped at 500), Fisher-Yates shuffle, and quiz reducer state machine with feedback support -- 26 unit tests, zero DOM coupling beyond globals**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-11T15:23:33Z
- **Completed:** 2026-04-11T15:27:59Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments
- Active-tab timer that only counts time while the tab is visible and focused, using monotonic performance.now()
- localStorage outbox with FIFO ordering, 500-entry cap, single-flight flush, and graceful JSON corruption handling
- Quiz reducer covering full lifecycle: idle -> loading -> playing -> revealed -> finished, including per-question feedback reactions
- 26 unit tests across 4 test files, all passing via TDD (red-green per task)

## Task Commits

Each task was committed atomically:

1. **Task 1: Active-tab timer** - `f5741f8` (feat)
2. **Task 2: Outbox (localStorage FIFO queue)** - `bc04ae8` (feat)
3. **Task 3: Shuffle + quiz reducer state machine** - `7e10e90` (feat)

## Files Created/Modified
- `apps/web/src/lib/activeTimer.ts` - createActiveTimer factory with start/pause/reset/elapsedMs/destroy
- `apps/web/src/lib/activeTimer.test.ts` - 7 tests: timing, pause, blur/focus, visibility, reset, destroy
- `apps/web/src/lib/outbox.ts` - enqueue/peek/size/flushOutbox with typed keys and cap
- `apps/web/src/lib/outbox.test.ts` - 7 tests: FIFO, cap, flush failure, concurrency, bad JSON
- `apps/web/src/lib/shuffle.ts` - Fisher-Yates shuffle, pure, does not mutate input
- `apps/web/src/lib/shuffle.test.ts` - 4 tests: length, completeness, immutability, determinism
- `apps/web/src/state/quiz.ts` - quizReducer + types + selectScore
- `apps/web/src/state/quiz.test.ts` - 8 tests: all phase transitions, feedback, score calculation

## Decisions Made
- Used performance.now() for monotonic timing (not affected by clock changes)
- Outbox storage keys versioned with `:v1` suffix to allow future migration
- Quiz reducer uses TypeScript discriminated unions for type-safe phase access
- Reducer has no React imports -- pure function, ready for Phase 7 lift

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Created minimal scaffold for test infrastructure**
- **Found during:** Pre-task setup
- **Issue:** Plan 02.2-01 (wave 0 dependency) had not yet committed its scaffold. No apps/web/ directory, no package.json, no vitest config existed.
- **Fix:** Created minimal package.json, tsconfig.json, vitest.config.ts, and test setup to unblock test execution. These files will be overwritten/merged when Plan 01's agent commits.
- **Files modified:** apps/web/package.json, apps/web/tsconfig.json, apps/web/vitest.config.ts, apps/web/src/test/setup.ts
- **Verification:** All 26 tests run and pass
- **Committed in:** Not committed separately (scaffold files are temporary bridge)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Scaffold was necessary to run tests. The actual plan modules (timer, outbox, shuffle, reducer) match the plan exactly.

## Issues Encountered
None beyond the scaffold dependency noted above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All four pure modules ready for consumption by Plan 05 (setup screen) and Plan 06 (play screen)
- Quiz reducer is the Phase 7 lift candidate -- no React imports, fully portable
- Active timer ready for per-question timing in play screen
- Outbox ready for offline-first insert pattern

---
*Phase: 02.2-web-quiz-v1-feedback-collection-inserted*
*Completed: 2026-04-11*

## Self-Check: PASSED
