---
phase: 02.2
plan: 09
type: execute
wave: 3
depends_on: ["02.2-01"]
files_modified:
  - apps/web/README.md
  - .planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-DEPLOY.md
autonomous: false
requirements: [WEB-14]
must_haves:
  truths:
    - "apps/web/ deploys to a Cloudflare Pages project on *.pages.dev"
    - "Build env vars VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY are set in Pages"
    - "SPA fallback works (refreshing /play returns index.html, not 404)"
    - "Service-role key is NOT in the browser bundle"
  artifacts:
    - path: "apps/web/README.md"
      provides: "Deploy instructions"
    - path: ".planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-DEPLOY.md"
      provides: "One-page deploy record"
  key_links:
    - from: "Cloudflare Pages project"
      to: "apps/web/dist"
      via: "build command"
      pattern: "build|dist"
---

<objective>
Ship the web app to Cloudflare Pages on a `*.pages.dev` subdomain and document how. No custom domain. Dashboard-driven setup — no wrangler.toml required for v1.

Purpose: the quiz is actually playable by friends and family.
Output: live URL + README + deploy record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-RESEARCH.md
@.planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-CONTEXT.md
@apps/web/public/_redirects
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write deploy docs</name>
  <read_first>
    - .planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-RESEARCH.md (§Monorepo Layout — Cloudflare Pages build config, §Env vars for Supabase URL + anon key)
    - .planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-CONTEXT.md (D-16, D-17)
  </read_first>
  <files>apps/web/README.md, .planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-DEPLOY.md</files>
  <action>
Create `apps/web/README.md` with:

```markdown
# Pub Quiz — Web

The plain web quiz. Vite + React + TypeScript + Supabase, deployed to Cloudflare Pages.

## Local dev

1. Copy `.env.local.example` to `.env.local` and fill in:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY` (public anon key — never the service role key)
2. `npm install`
3. `npm run dev` — serves on http://localhost:5173
4. `npm test` — vitest in watch mode
5. `npm run test:run` — single-shot test run for CI
6. `npm run build` — production build into `dist/`

## Deploy to Cloudflare Pages

Dashboard-driven (no wrangler.toml needed for v1):

1. Cloudflare dashboard → Workers & Pages → Create → Pages → Connect to Git.
2. Pick this repo, branch `main`.
3. Framework preset: **None** (Vite is not in the preset list at time of writing — manual config is fine).
4. Build settings:
   - **Root directory:** `apps/web`
   - **Build command:** `npm run build`
   - **Build output directory:** `dist`
5. Environment variables (Production AND Preview):
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
6. Save and deploy. First deploy takes ~2 minutes.
7. Pages assigns a `*.pages.dev` subdomain. That is the v1 URL.

## Verifying the deploy

- Open the `*.pages.dev` URL — setup screen renders.
- Deep-link `/play` — the SPA fallback (`public/_redirects`) serves `index.html`, the Play route's "no state" guard redirects you back to `/`.
- Play a full quiz of 5 questions. Check Supabase Studio → `question_plays` has new rows.

## Things NOT to do

- Never put the Supabase **service role** key in env vars or in the bundle. Only the anon key belongs here.
- Never add `VITE_*` secrets to `git` — `apps/web/.env.local` is gitignored for this reason.
- Never chain `.select()` onto `.insert()` on `question_plays` or `quiz_sessions` — insert-only RLS has no SELECT policy and the client will wrongly report a failure. See `apps/web/src/lib/plays.ts` for the correct pattern.
```

Create `.planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-DEPLOY.md`:

```markdown
# Phase 02.2 — Deploy record

## Cloudflare Pages project
- **Project name:** pub-quiz-web
- **URL:** https://pub-quiz-web.pages.dev (update once live)
- **Branch:** main
- **Root directory:** apps/web
- **Build command:** npm run build
- **Output:** dist
- **Env vars:** VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY (both set in Production and Preview)

## First deploy
- Date: TBD (human fills in)
- Commit: TBD
- Deploy URL: TBD

## Sanity checks after deploy
- [ ] Setup screen loads at `/`
- [ ] `/play` direct link redirects to `/` (SPA fallback + state guard both work)
- [ ] Play one full quiz of 5 easy general questions
- [ ] Supabase Studio shows new rows in `question_plays` and `quiz_sessions`
- [ ] `view-source:` of the deployed page does NOT contain the service role key
```
  </action>
  <verify>
    <automated>test -f apps/web/README.md && test -f .planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-DEPLOY.md && grep -q "pub-quiz-web.pages.dev" .planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/README.md` exists and documents local dev + deploy + anti-patterns
    - `.planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-DEPLOY.md` exists with project config table
    - Both files mention the insert-only RLS / `.select()` trap warning
  </acceptance_criteria>
  <done>Deploy docs written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human deploys to Cloudflare Pages and verifies live URL</name>
  <what-built>The docs. This checkpoint asks the user to follow them.</what-built>
  <how-to-verify>
    1. Follow `apps/web/README.md` → "Deploy to Cloudflare Pages" steps.
    2. Wait for the first deploy to succeed.
    3. Open the `*.pages.dev` URL in a browser.
    4. Play a full quiz of 5 easy General questions, all four feedback buttons at least once across the quiz.
    5. Submit end-screen rating.
    6. Open Supabase Studio → `question_plays` table → confirm 5 new rows with `session_id = auth.uid()`.
    7. Open `quiz_sessions` → confirm 1 new row.
    8. View page source of the deployed page → confirm the Supabase service-role key is NOT present anywhere (only the anon key).
    9. Fill in the deploy record in `.planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-DEPLOY.md` — URL, date, commit.
    10. Reply "approved <url>" with the live URL.
  </how-to-verify>
  <resume-signal>Reply "approved <url>" or describe issues.</resume-signal>
  <acceptance_criteria>
    - Live URL reachable
    - 5 question_plays and 1 quiz_sessions row visible in Supabase
    - Service role key not in bundle
    - 02.2-DEPLOY.md filled in
  </acceptance_criteria>
  <done>Phase 02.2 is live and collecting data.</done>
</task>

</tasks>

<verification>
Human-verified live URL + first 5 rows in `question_plays`.
</verification>

<success_criteria>
- `*.pages.dev` URL serves the quiz
- Real plays land in Supabase
- No secrets in the bundle
</success_criteria>

<output>
Create `.planning/phases/02.2-web-quiz-v1-feedback-collection-inserted/02.2-09-SUMMARY.md`
</output>
