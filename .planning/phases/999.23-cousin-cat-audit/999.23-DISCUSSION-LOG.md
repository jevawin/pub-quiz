# Phase 999.23 — Discussion Log

**Mode:** auto (no per-question user prompts; Claude picked HOW decisions based on SPEC.md + 999.22 artifacts).

## Areas covered

| # | Area | Options considered | Decision | Rationale |
|---|------|--------------------|----------|-----------|
| 1 | Mistag worklist seeding | (a) parse subagent logs, (b) heuristic SQL + PROGRESS.md, (c) pure manual | (b) hybrid | Heuristic SQL surfaces beyond the 4 explicit examples; PROGRESS.md backfills known cases. Subagent logs not preserved per-batch. |
| 2 | Apply mechanism | (a) Supabase MCP interactive, (b) new `apply-cousin-changes.ts` script | (b) | Mirrors 999.22 `apply-chain-rows.ts` pattern. Idempotent + dry-runnable + audit-loggable. MCP harder to script. |
| 3 | Batch presentation | row-by-row chat questions vs markdown table per batch | markdown table per batch (10 cap-5, 15 mistag) | User scans table fast, confirms rows or per-row edits. Lower turn count. |
| 4 | Cap-5 prune strategy | random pick vs lowest-score-cousin-first | lowest-score-cousin-first; never drop chain ancestors; GK only if <20 | Deterministic, justifiable, matches 999.22 GK_THRESHOLD. |
| 5 | Audit log shape | SQL log table vs JSONL file | JSONL `data/audit-changes.jsonl` | Fits acceptance criterion "one line per DB write"; no migration. |
| 6 | `pop-culture` cousin policy | broad auto-add vs strict whitelist (MCU/Disney/SW/mainstream gaming) | strict whitelist | ROADMAP §999.23 strict cousin rule; avoids tag inflation. |
| 7 | Primary cat moves | new op type vs separate workflow | new `set_primary` op in apply script | Single-tool ergonomics for the conversational review loop. |

## Deferred (captured, not acted on)

- Q dedup pass for near-duplicate pairs flagged by 999.22.
- Bulk cousin sweep across all 2848 Qs (only 19 + ~30 in scope here).
- Automated Sonnet cousin suggester (cost vs benefit unclear pre-manual-audit).

## Scope guardrail events

None. SPEC.md kept boundaries tight; no scope-creep to redirect.
