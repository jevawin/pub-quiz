---
phase: 999.23-cousin-cat-audit
plan: 05
type: execute
wave: 5
depends_on: [999.23-03, 999.23-04]
files_modified:
  - .planning/phases/999.23-cousin-cat-audit/999.23-VERIFICATION.md
  - .planning/phases/999.23-cousin-cat-audit/SUMMARY.md
autonomous: true
requirements: [R1, R2, R3, R4, R5]
must_haves:
  truths:
    - "build-chain-backfill-worklist.ts reports 0 remaining cap-5 Qs"
    - "Cap-5 invariant SQL returns 0 rows project-wide"
    - "Sample audit of 10 random touched Qs shows correct categorisation"
    - "SUMMARY.md captures phase outcome, commit list, audit log line counts"
    - "No changes under supabase/migrations/, pipeline/src/agents/calibrator.ts, or pipeline/src/lib/category-chain.ts (per AC7, AC8)"
  artifacts:
    - path: ".planning/phases/999.23-cousin-cat-audit/999.23-VERIFICATION.md"
      provides: "Pass/fail evidence against the 8 SPEC acceptance criteria"
    - path: ".planning/phases/999.23-cousin-cat-audit/SUMMARY.md"
      provides: "Phase outcome record consumed by future phases via history-digest"
  key_links:
    - from: "999.23-VERIFICATION.md"
      to: "999.23-SPEC.md acceptance criteria AC1-AC8"
      via: "1:1 evidence per criterion"
      pattern: "AC[1-8]"
---

<objective>
End-of-phase verification + SUMMARY. Run the four exit-criteria checks (build-chain-backfill-worklist.ts == 0; cap-5 SQL invariant == 0; cousin_reason completeness; no diff in protected files), random-sample audit 10 touched Qs, write VERIFICATION.md mapping evidence to AC1-AC8, write SUMMARY.md.

Purpose: Closes R4 (cap-5 invariant) and R5 (batched commit format) via objective evidence; produces the SUMMARY file consumed by future phases via history-digest.
Output: Two markdown artifacts; phase ready to mark complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.23-cousin-cat-audit/999.23-SPEC.md
@.planning/phases/999.23-cousin-cat-audit/999.23-CONTEXT.md
@pipeline/src/scripts/build-chain-backfill-worklist.ts
@pipeline/src/scripts/sample-chain-rows.ts
@.planning/phases/999.23-cousin-cat-audit/data/mistag-worklist.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all exit-criteria checks and write VERIFICATION.md</name>
  <read_first>
    - .planning/phases/999.23-cousin-cat-audit/999.23-SPEC.md (AC1-AC8 verbatim)
    - data/audit-changes.jsonl (final state)
    - pipeline/src/scripts/build-chain-backfill-worklist.ts (run signature)
    - pipeline/src/scripts/sample-chain-rows.ts (sampling pattern to mirror)
  </read_first>
  <files>
    .planning/phases/999.23-cousin-cat-audit/999.23-VERIFICATION.md
  </files>
  <action>
    Execute the following checks and capture stdout/SQL output verbatim into VERIFICATION.md:

    1. **AC1 — chain backfill worklist clean**: `npx tsx pipeline/src/scripts/build-chain-backfill-worklist.ts 2>&1 | tee /tmp/999.23-ac1.txt`. Capture full output. Must report 0 remaining Qs.

    2. **AC2 — mistag worklist completeness**: `jq 'length' .planning/phases/999.23-cousin-cat-audit/data/mistag-worklist.json` ≥ 30; `jq '[.[] | select(.decision == null)] | length'` == 0; `jq 'group_by(.decision) | map({decision: .[0].decision, n: length})'` for breakdown.

    3. **AC3 — every fix has audit line**: `comm -23 <(jq -r '.[] | select(.decision == "fix") | .question_id' .planning/phases/999.23-cousin-cat-audit/data/mistag-worklist.json | sort -u) <(jq -r 'select(.batch_id | startswith("mistag-")) | .question_id' data/audit-changes.jsonl | sort -u) | wc -l` returns 0. List any orphan question_ids if non-zero.

    4. **AC4 — cap-5 invariant project-wide**: Run via service-role read query (small inline tsx script using createSupabaseClient): `SELECT question_id, COUNT(*) AS n FROM question_categories GROUP BY question_id HAVING COUNT(*) > 5;` — must return 0 rows. Save full result.

    5. **AC5 — every cousin add has cousin_reason**: `jq -c 'select(.op == "insert" and (.chain_ancestor // false) == false) | select((.cousin_reason // "") == "")' data/audit-changes.jsonl | wc -l` returns 0.

    6. **AC6 — at least one commit per batch**: `git log --oneline | grep -cE "999\\.23.*(cap5|mistag)-[0-9]+"` ≥ 4 (cap5-001, cap5-002, mistag-001, mistag-002+ minimum).

    7. **AC7 — no migration diff**: `git diff --stat $(git merge-base HEAD main)..HEAD -- supabase/migrations/ | wc -l` returns 0 (no files changed under that path during the phase). Adapt the base ref to whatever marks phase start (last commit on main before phase began — capture during run).

    8. **AC8 — no diff to calibrator.ts or category-chain.ts**: `git diff --stat <phase-base>..HEAD -- pipeline/src/agents/calibrator.ts pipeline/src/lib/category-chain.ts | wc -l` returns 0.

    9. **Sample audit**: Pick 10 random question_ids from `data/audit-changes.jsonl` (shuf | head -10). For each: query `SELECT q.id, q.question_text, array_agg(c.slug ORDER BY qc.estimate_score DESC) AS slugs FROM questions q JOIN question_categories qc ON qc.question_id = q.id JOIN categories c ON c.id = qc.category_id WHERE q.id = '...' GROUP BY q.id;`. Eyeball that the slug list looks correct given the audit log entries for that Q. Record results in VERIFICATION.md.

    Write `.planning/phases/999.23-cousin-cat-audit/999.23-VERIFICATION.md` with one section per AC1-AC8 + a "Sample Audit" section, each containing: command run, raw output, pass/fail verdict.

    If any check fails: STOP, do NOT write SUMMARY.md, surface failure to user, propose fix.
  </action>
  <verify>
    <automated>test -f .planning/phases/999.23-cousin-cat-audit/999.23-VERIFICATION.md && grep -cE "PASS|FAIL" .planning/phases/999.23-cousin-cat-audit/999.23-VERIFICATION.md | awk '{exit ($1 < 9)}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/999.23-cousin-cat-audit/999.23-VERIFICATION.md` passes
    - File contains a section header for each of AC1, AC2, AC3, AC4, AC5, AC6, AC7, AC8 (grep `^## AC[1-8]`)
    - File contains a "Sample Audit" section with 10 sampled question_ids
    - Every section ends with explicit `**Verdict:** PASS` or `**Verdict:** FAIL` line
    - Total PASS count ≥ 9 (8 ACs + 1 sample audit)
    - `grep -c "FAIL" .planning/phases/999.23-cousin-cat-audit/999.23-VERIFICATION.md` returns 0 (no failures — if any, plan execution halts)
  </acceptance_criteria>
  <done>
    All 8 acceptance criteria PASS, sample audit clean, VERIFICATION.md committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SUMMARY.md and commit</name>
  <read_first>
    - .planning/phases/999.23-cousin-cat-audit/999.23-VERIFICATION.md (just-written)
    - .planning/phases/999.23-cousin-cat-audit/999.23-SPEC.md (goal + requirements for outcome framing)
    - $HOME/.claude/get-shit-done/templates/summary.md (required SUMMARY shape)
    - data/audit-changes.jsonl (line count for stats)
  </read_first>
  <files>
    .planning/phases/999.23-cousin-cat-audit/SUMMARY.md
  </files>
  <action>
    Write `.planning/phases/999.23-cousin-cat-audit/SUMMARY.md` per the template at $HOME/.claude/get-shit-done/templates/summary.md. Required content:

    - **Outcome**: 19 cap-5 Qs resolved; ~30 mistag candidates reviewed (split by fix/keep/defer counts); cousin tags pruned + added per strict rule; cap-5 invariant intact.
    - **Stats**: Total audit log lines, total deletes, total inserts, total set_primary, count of pop-culture cousin adds, count of primary moves, commits per batch.
    - **Files added**: pipeline/src/scripts/apply-cousin-changes.ts, pipeline/src/scripts/seed-mistag-worklist.ts, data/audit-changes.jsonl (new), .planning/phases/999.23-cousin-cat-audit/data/* (worklist + per-batch JSONs).
    - **Files NOT changed** (per AC7, AC8): supabase/migrations/, pipeline/src/agents/calibrator.ts, pipeline/src/lib/category-chain.ts.
    - **Decisions exercised**: D1 (heuristic seeding), D2 (apply mechanism), D3 (batch table), D4 (prune heuristic — note any judgment calls beyond the heuristic), D5 (audit log), D6 (pop-culture additions — list every Q that received pop-culture), D7 (set_primary moves — list every Q moved), D8 (no pipeline diffs confirmed).
    - **Deferred items**: Q dedup pass (out of scope per CONTEXT.md), bulk cousin sweep, automated cousin suggester.
    - **Commits**: List every commit on main with subject line referencing 999.23 (output of `git log --oneline | grep 999.23`).
    - **Next phase**: per ROADMAP §C2 — 999.18 next.

    Then commit: `git add .planning/phases/999.23-cousin-cat-audit/SUMMARY.md .planning/phases/999.23-cousin-cat-audit/999.23-VERIFICATION.md && git commit -m "phase(999.23): COMPLETE — cousin/cat audit done, 19 cap-5 + ${N} mistag Qs resolved, all 8 ACs PASS"`.

    Update `.planning/STATE.md`: bump completed_phases counter, set status line to `999.23 cousin/cat audit COMPLETE; next is 999.18`.
  </action>
  <verify>
    <automated>test -f .planning/phases/999.23-cousin-cat-audit/SUMMARY.md && git log --oneline -1 | grep -qE "999\\.23.*COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/999.23-cousin-cat-audit/SUMMARY.md` passes
    - SUMMARY.md contains sections for: Outcome, Stats, Files added, Files NOT changed, Decisions, Deferred, Commits, Next phase (grep each header)
    - SUMMARY.md references all decision IDs D1-D8 (`grep -cE "D[1-8]" SUMMARY.md` ≥ 8)
    - STATE.md status line updated: `grep "999.23.*COMPLETE" .planning/STATE.md` matches
    - Final commit subject matches `999\.23.*COMPLETE`
  </acceptance_criteria>
  <done>
    SUMMARY.md + VERIFICATION.md committed; STATE.md bumped; phase 999.23 ready to mark complete in ROADMAP.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → Supabase service-role read | Read-only verification queries |
| git diff scope | Catches accidental writes to protected paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99923-14 | Repudiation | SUMMARY drift from actual outcome | mitigate | SUMMARY built directly from VERIFICATION.md + git log + jq counts (objective) |
| T-99923-15 | Tampering | undetected diff in protected files | mitigate | AC7/AC8 explicit git diff checks against phase-base ref |
</threat_model>

<verification>
- VERIFICATION.md has 9 PASS verdicts and 0 FAIL.
- SUMMARY.md committed with COMPLETE subject.
- STATE.md reflects 999.23 complete.
</verification>

<success_criteria>
All 8 SPEC acceptance criteria pass. Phase shippable.
</success_criteria>

<output>
SUMMARY.md is itself the output artifact for this plan.
</output>
