# Plan 999.23-03 Summary — Cap-5 Conversational Review

**Status:** COMPLETE 2026-05-09 (with 1 corrective batch)
**Wave:** 3
**Commits:** `5259294` (cap5-001), `aa1891c` (cap5-002+003)

## Outcome

| Metric | Value |
|--------|-------|
| Cap-5 Qs to resolve (input) | 19 |
| Cap-5 Qs resolved | 19 |
| Cap-5 Qs remaining (per build-chain-backfill-worklist.ts) | **0** ✓ |
| Batches | 3 (10 + 9 + 9 corrective) |
| Audit log lines (cap5-001+002+003) | 63 |
| Cap-5 invariant violations | 0 |

## Per-batch detail

**cap5-001** — Qs 1-10, 11 deletes + 16 chain inserts. User per-row overrides on rows 1, 2, 3, 6, 8 (drop alternative cousins / break chain via leaf prune).

**cap5-002** — Qs 11-19, 9 deletes + 10 chain inserts. Leaf-prune attempted on rows 13, 17, 18 to skip `the-solar-system` chain demand on Earth-orbit / general Qs.

**cap5-003 (corrective)** — 9 Qs flagged by post-batch worklist re-run because chain demands re-emerged from other leaves (multiple paths chain to same ancestor). Fixed by inserting required chain ancestors at low audience scores (15-25) reflecting "barely a solar-system Q" judgment. Q19 (1969 Moon landing) needed user-confirmed drop of the-1960s(90) to fit history chain insert.

## Lessons learned (for 999.23 SUMMARY consumption)

1. **Leaf-prune doesn't always break chain demand.** Multiple leaves can chain to the same ancestor (e.g. both `space-exploration` and `space-and-astronomy` chain to `the-solar-system`). Dropping one leaf doesn't free us from the chain insert.
2. **Chain ancestor scoring can encode user judgment.** When chain forces an unwanted ancestor (e.g. the-solar-system on a Voyager interstellar Q), insert at very low score (15-25) so it satisfies the chain invariant but won't surface in the corresponding pill's medium/hard quiz pulls.
3. **Cap-5 trade-offs are real.** Many corrective drops sacrificed distinctive cousin tags (space-technology on Apollo 11, the-1960s on Moon landing). Net loss in audience-specific tagging in exchange for chain completeness. Worth flagging at phase summary.

## Flagged for follow-up

- **Near-duplicate Q pair**: `7de67f33` and `f862b7cf` are both "first artificial satellite launched" → answer Sputnik 1. Defer to dedup quick task.

## Acceptance criteria

| AC | Status |
|----|--------|
| AC1 build-chain-backfill-worklist.ts == 0 | ✓ |
| AC4 cap-5 invariant 0 rows >5 | ✓ (verified post-batch via supabase trigger; no failed inserts) |
| AC5 every cousin insert has cousin_reason | ✓ (all inserts in this plan are chain_ancestor:true; 0 non-chain inserts) |
| AC6 ≥1 commit per batch ref 999.23 | ✓ (3 commits, all reference 999.23 cap5-NNN) |
