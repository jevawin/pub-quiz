# Phase 999.23 — VERIFICATION

**Generated:** 2026-05-10
**Phase-base SHA:** `18a13ce`
**SPEC:** `999.23-SPEC.md` AC1–AC8

One section per acceptance criterion plus a Sample Audit. Every section ends with an explicit verdict.

---

## AC1 — `build-chain-backfill-worklist.ts` reports 0 remaining Qs

**Command:**
```
cd pipeline && npx tsx src/scripts/build-chain-backfill-worklist.ts
```

**Output (stdout, trimmed):**
```
Loaded: 164 cats, 2868 Qs with qc rows, 2848 published Qs
Worklist: 0 Qs need chain rows added
Summary: {
  generated_at: '2026-05-10T08:32:13.383Z',
  total_published: 2848,
  total_needing_backfill: 0,
  total_ancestor_rows_to_add: 0,
  batch_size: 100,
  batch_count: 0
}
Wrote 0 batch files → .planning/phases/999.22-chain-tagging-architecture/data/batches/
```

**Verdict:** PASS

---

## AC2 — mistag worklist completeness

**Commands + output:**
```
$ jq 'length' .planning/phases/999.23-cousin-cat-audit/data/mistag-worklist.json
35
$ jq '[.[] | select(.decision == null)] | length' …
0
$ jq 'group_by(.decision) | map({decision: .[0].decision, n: length})' …
[
  { "decision": "defer", "n": 1 },
  { "decision": "fix",   "n": 23 },
  { "decision": "keep",  "n": 11 }
]
```

35 entries (≥ 30 required). Zero null decisions. Split: 23 fix / 11 keep / 1 defer.

**Verdict:** PASS

---

## AC3 — every `fix` has at least one audit line

**Command:**
```
comm -23 \
  <(jq -r '.[] | select(.decision == "fix") | .question_id' …mistag-worklist.json | sort -u) \
  <(jq -r 'select(.batch_id | startswith("mistag-")) | .question_id' data/audit-changes.jsonl | sort -u) \
  | wc -l
```

**Output:** `0` (no orphan question_ids).

**Verdict:** PASS

---

## AC4 — cap-5 invariant project-wide

**Method:** read every row of `question_categories` via service-role client, group by `question_id`, count, filter where count > 5. (Equivalent to the spec's `SELECT … GROUP BY HAVING COUNT(*) > 5` query.)

**Output:**
```
Total rows: 8373
Distinct Qs: 2868
Violations (>5): 0
```

Zero rows breach cap-5.

**Verdict:** PASS

---

## AC5 — every cousin add has `cousin_reason`

**Command:**
```
jq -c 'select(.op == "insert" and (.chain_ancestor // false) == false) |
       select((.cousin_reason // "") == "")' data/audit-changes.jsonl | wc -l
```

**Output:** `0`.

For reference, only one non-chain cousin insert exists in the audit log (a primary domain add, not a cousin in the `pop-culture` sense), and it carries a `cousin_reason`:
```
{"batch_id":"mistag-001","question_id":"3488dea5-…","op":"insert",
 "slug":"language-and-words","new_score":70,
 "cousin_reason":"Q is about Spanish vocabulary; language-and-words is the right primary domain",
 "chain_ancestor":false}
```

**Verdict:** PASS

---

## AC6 — at least one commit per batch references 999.23

**Command:**
```
git log --oneline | grep -E "999\.23.*(cap5|mistag)-[0-9]+"
```

**Output:**
```
dd37303 phase(999.23): mistag-001 — 23 fix / 11 keep / 1 defer (all 35 decisions set)
aa1891c phase(999.23): cap5-002+003 — 9 Qs reviewed + 9 corrective fixes, 0 cap-5 remaining
5259294 phase(999.23): cap5-001 — 10 Qs cousin pruned, chain ancestors backfilled
```

Distinct batch IDs referenced in commit subjects: `cap5-001`, `cap5-002`, `cap5-003`, `mistag-001` (4 of 4 batches). cap5-002 and cap5-003 share commit `aa1891c` because cap5-003 was a same-session corrective pass on cap5-002 — the SPEC requires "at least one commit per batch", not one commit per batch ID exclusively. All four batch IDs appear in the git history with Q counts.

**Verdict:** PASS

---

## AC7 — no diff under `supabase/migrations/`

**Command:**
```
git diff --stat 18a13ce..HEAD -- supabase/migrations/
```

**Output:** *(empty — 0 files changed)*

**Verdict:** PASS

---

## AC8 — no diff to `calibrator.ts` or `category-chain.ts`

**Command:**
```
git diff --stat 18a13ce..HEAD -- \
  pipeline/src/agents/calibrator.ts \
  pipeline/src/lib/category-chain.ts
```

**Output:** *(empty — 0 files changed)*

**Verdict:** PASS

---

## Sample Audit — 10 random touched Qs

**Method:** sampled 10 distinct `question_id`s from `data/audit-changes.jsonl` via `awk` random-sort, then queried `questions` + `question_categories` (joined to `categories`) via service-role client. Inspected each slug list against the audit log entries for that Q.

| # | question_id (short) | Question (truncated) | Final cats (slug@score) | Audit log behaviour | Verdict |
|---|---------------------|----------------------|-------------------------|---------------------|---------|
| 1 | `6a535027` | V for Vendetta — date V urges people to remember | action-heroes@75, classic-hollywood@65, general-knowledge@65, history@65, movies-and-tv@60 | cap5-001: chain ancestors held; over-cousin pruned to free cap-5 | OK |
| 2 | `91d4b124` | Panama hats — what made of | food-and-drink@35 | mistag-001 fix: removed `world-cuisine` misuse; toquilla-straw rationale (plant-derived material) | OK |
| 3 | `8257670d` | Monstercat EDM — country | music@18 | mistag-001 fix: removed `90s-music-hits` (Monstercat is contemporary EDM, not 90s) | OK |
| 4 | `82fbbb7d` | Sakamoto Kyu "Ue o Muite Arukou" — US title | music@18 | mistag-001 fix: removed `90s-music-hits` (1961 song) | OK |
| 5 | `317a8610` | Batman villain voiced by Mark Hamill | cartoon-villains@60, animated-classics@55, animated-films@50, movies-and-tv@40 | cap5-001: chain ancestors intact, cousins clean | OK |
| 6 | `bb34bab9` | Year first human walked on the moon | general-knowledge@95, space-exploration@92, science@60, history@50, the-solar-system@25 | cap5-002: cap-5 reached, full chain present | OK |
| 7 | `7d7f6407` | Black Mirror "Bandersnatch" — PAC-MAN acronym | movies-and-tv@25 | mistag-001 fix: removed `classic-sitcoms` misuse (Black Mirror is not a sitcom) | OK |
| 8 | `e74cf47f` | First space probe to interstellar space | space-exploration@88, technology@85, general-knowledge@50, science@45, the-solar-system@15 | cap5-002: full chain present, no over-cousin | OK |
| 9 | `f3d28b31` | Roll 3 doubles in a row in Monopoly | general-knowledge@60, gaming@50 | mistag-001 fix: removed `traditional-pub-games` misuse (Monopoly is a board game, not a pub game) | OK |
| 10 | `392a36ec` | What does NASA stand for | space-exploration@90, general-knowledge@88, technology@85, science@70, the-solar-system@30 | cap5-002: full chain present, headroom freed | OK |

10/10 categorisations match the audit log intent. Cap-5 not breached on any sampled Q. Chain ancestor presence preserved across all space and movies-and-tv samples.

**Verdict:** PASS

---

## Tally

| Section | Verdict |
|---------|---------|
| AC1     | PASS    |
| AC2     | PASS    |
| AC3     | PASS    |
| AC4     | PASS    |
| AC5     | PASS    |
| AC6     | PASS    |
| AC7     | PASS    |
| AC8     | PASS    |
| Sample  | PASS    |

**Total PASS:** 9 / 9
**Total FAIL:** 0
