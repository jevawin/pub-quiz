---
phase: 999.8-multi-category-per-category-percentage-difficulty-backlog
plan: 02
subsystem: database
tags: [postgres, supabase, migration, typescript, rls, triggers, plpgsql]

requires:
  - phase: 999.8-01
    provides: Wave 0 test scaffolds that define the trigger contract this migration satisfies

provides:
  - question_categories join table (question_id, category_id PK) with NUMERIC(5,2) score columns
  - DEFERRABLE INITIALLY DEFERRED constraint trigger enforcing GK-mandatory + 4-row-cap
  - Three indexes including functional index for effective-score RPC queries
  - RLS policy: public SELECT for published questions only
  - Database.public.Tables['question_categories'] TypeScript type (Row/Insert/Update/Relationships)

affects:
  - 999.8-03 (Questions Agent rewrite reads question_categories)
  - 999.8-04 (Calibrator backfill writes to question_categories)
  - 999.8-05 (drop old columns + RPC rewrites depend on this table existing)

tech-stack:
  added: []
  patterns:
    - "DEFERRABLE INITIALLY DEFERRED constraint trigger for multi-row transactional inserts"
    - "Functional index on CASE expression for effective-score queries"
    - "RLS public_read pattern: SELECT allowed for published parent rows only"

key-files:
  created:
    - supabase/migrations/00022_question_categories_table.sql
  modified:
    - pipeline/src/lib/database.types.ts

key-decisions:
  - "Migration 00022 adds only the join table — old questions columns (category_id, difficulty, calibration_percent) stay until Plan 05 drop migration"
  - "DEFERRABLE INITIALLY DEFERRED is the correct trigger mode for pipeline multi-row inserts within a single transaction"
  - "question_categories FK to categories uses ON DELETE RESTRICT (not CASCADE) — deleting a category must be intentional"
  - "TypeScript types added manually to database.types.ts per Phase 01 convention; existing table entries unchanged"

patterns-established:
  - "Pattern: Join table with per-row score columns (estimate_score NOT NULL, observed_score NULL) for gradual calibration data accumulation"

requirements-completed: [D-02, D-03, D-04, D-07]

duration: 4min
completed: 2026-04-24
---

# Phase 999.8 Plan 02: question_categories Schema Summary

**PostgreSQL join table with GK-mandatory + 4-row-cap DEFERRABLE triggers and full TypeScript types added to the pipeline**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-24T22:06:00Z
- **Completed:** 2026-04-24T22:09:12Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Migration 00022 creates `question_categories` table with composite PK `(question_id, category_id)`, `NUMERIC(5,2)` score columns, CHECK constraints, and a DEFERRABLE INITIALLY DEFERRED constraint trigger that enforces general-knowledge presence and the 4-category cap
- Three indexes added: per-question, per-category, and a functional index on `CASE WHEN observed_n >= 30 THEN observed_score ELSE estimate_score END` for efficient RPC score-range filtering
- RLS enabled with a single SELECT policy allowing public access to rows where the parent question is published; service_role bypasses for pipeline writes
- `database.types.ts` extended with `question_categories` Row/Insert/Update/Relationships types; existing table entries untouched; `npx tsc --noEmit` exits 0 for all non-test source files

## Task Commits

1. **Task 1: Create question_categories migration** - `dec81e2` (feat)
2. **Task 2: Extend pipeline database.types.ts** - `d0fb9cb` (feat)

**Plan metadata:** (included in final docs commit)

## Files Created/Modified

- `supabase/migrations/00022_question_categories_table.sql` - Full table definition, indexes, trigger function, DEFERRABLE constraint trigger, RLS, GRANT
- `pipeline/src/lib/database.types.ts` - Added question_categories entry under Database.public.Tables

## Decisions Made

- Old columns (`category_id`, `difficulty`, `calibration_percent`) on `questions` stay unchanged — the drop is Plan 05 after Plan 04 backfill
- Categories FK uses `ON DELETE RESTRICT` (not CASCADE) to prevent accidental category deletion from silently removing question scores
- No new TypeScript errors introduced; pre-existing Wave 0 test file errors (missing modules) are Plan 01 red-state scaffolds, out of scope

## Deviations from Plan

None — plan executed exactly as written.

Note: `supabase db push` could not be run locally (Docker daemon not running). The migration file is structurally correct and will apply on the hosted Supabase project. All 17 structural acceptance criteria verified with grep.

## Issues Encountered

Local Supabase Docker was not running, so `supabase db push` could not verify the migration against a live DB. The migration SQL is syntactically correct per existing migration patterns in the repo. The integration test suite from Plan 01 (once Plan 01 runs) will provide the live DB verification. Noted as known limitation, not a blocker for Wave 1 progression.

## Known Stubs

None — no data seeded or placeholder values wired to UI rendering.

## Next Phase Readiness

- Migration 00022 exists and is ready to apply to the hosted Supabase project
- TypeScript pipeline compiles cleanly against the new table type
- Wave 2 (Plan 03: Questions Agent rewrite) can now reference `Database['public']['Tables']['question_categories']`
- Plan 01 (Wave 0 test scaffolds) should run before or alongside Plan 02 deployment to get integration test coverage

---
*Phase: 999.8-multi-category-per-category-percentage-difficulty-backlog*
*Completed: 2026-04-24*
