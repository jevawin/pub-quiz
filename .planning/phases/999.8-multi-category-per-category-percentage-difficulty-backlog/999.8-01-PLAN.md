---
phase: 999.8
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pipeline/src/__tests__/question-categories-trigger.integration.test.ts
  - pipeline/src/__tests__/observed-score-job.test.ts
  - pipeline/src/agents/__tests__/questions-multi.test.ts
  - pipeline/src/agents/__tests__/calibrator-multi.test.ts
  - pipeline/src/lib/__tests__/slug-converter.test.ts
  - pipeline/src/__tests__/random-published-questions-rpc.integration.test.ts
autonomous: true
requirements: [D-02, D-03, D-04, D-08, D-09, D-10, D-14, D-15]

must_haves:
  truths:
    - "Every Wave 0 test file exists with failing tests that define the expected contract for downstream waves"
    - "All six test files compile and run (red, not missing)"
  artifacts:
    - path: "pipeline/src/__tests__/question-categories-trigger.integration.test.ts"
      provides: "Trigger GK + 4-row-cap + DEFERRABLE tests"
    - path: "pipeline/src/agents/__tests__/questions-multi.test.ts"
      provides: "Questions Agent multi-category output tests"
    - path: "pipeline/src/agents/__tests__/calibrator-multi.test.ts"
      provides: "Calibrator multi-score output tests"
    - path: "pipeline/src/lib/__tests__/slug-converter.test.ts"
      provides: "snake_case ↔ kebab-case conversion tests"
    - path: "pipeline/src/__tests__/observed-score-job.test.ts"
      provides: "Nightly aggregation job tests"
    - path: "pipeline/src/__tests__/random-published-questions-rpc.integration.test.ts"
      provides: "RPC effective-score behaviour tests"
  key_links:
    - from: "test files"
      to: "pipeline/vitest.config.ts"
      via: "glob pattern picks up __tests__/*.test.ts"
      pattern: "vitest run"
---

<objective>
Create the failing test scaffolds for all downstream Wave 0 gaps identified in VALIDATION.md. These tests define the contracts that Waves 1-4 must satisfy. Every test MUST be red (either because implementation is missing, or because we deliberately import not-yet-existing modules). This is the Nyquist gate — no later task can claim complete without an automated verify.

Purpose: Enforce test-first discipline across the whole phase. Waves 1-4 turn these red tests green.
Output: 6 new vitest test files under pipeline/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.8-multi-category-per-category-percentage-difficulty-backlog/999.8-CONTEXT.md
@.planning/phases/999.8-multi-category-per-category-percentage-difficulty-backlog/999.8-RESEARCH.md
@.planning/phases/999.8-multi-category-per-category-percentage-difficulty-backlog/999.8-VALIDATION.md
@pipeline/vitest.config.ts
@pipeline/src/lib/schemas.ts
@pipeline/src/agents/calibrator.ts
@pipeline/src/agents/questions.ts

<interfaces>
Target shapes these tests will assert against (implemented in later waves):

```typescript
// pipeline/src/lib/slug-converter.ts — Wave 2
export function jsonKeyToSlug(key: string): string;   // "science_and_nature" → "science-and-nature"
export function slugToJsonKey(slug: string): string;  // "science-and-nature" → "science_and_nature"

// pipeline/src/lib/schemas.ts — extended in Wave 2
export const QuestionGeneratedSchema = z.object({
  question_text: z.string(),
  correct_answer: z.string(),
  distractors: z.array(z.string()).length(3),
  explanation: z.string(),
  category_slugs: z.array(z.string()).min(1).max(3),          // non-GK extras only
  category_scores: z.record(z.string(), z.number().min(0).max(100)),  // keys incl. general_knowledge
});

export const CalibratorScoreSchema = z.object({
  scores: z.record(z.string(), z.number().min(0).max(100)),
  reasoning: z.string(),
});

// pipeline/src/jobs/observed-score-refresh.ts — Wave 3
export async function refreshObservedScores(supabase: SupabaseClient): Promise<{ updated: number }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit test scaffolds — schemas, slug converter, agents</name>
  <files>pipeline/src/agents/__tests__/questions-multi.test.ts, pipeline/src/agents/__tests__/calibrator-multi.test.ts, pipeline/src/lib/__tests__/slug-converter.test.ts</files>
  <read_first>
    - pipeline/src/agents/questions.ts (current Questions Agent — understand existing test patterns)
    - pipeline/src/agents/calibrator.ts (current single-score Calibrator)
    - pipeline/src/lib/schemas.ts (current QuestionGeneratedSchema — being extended)
    - pipeline/vitest.config.ts (confirm test glob picks up __tests__/*.test.ts)
    - pipeline/src/agents/__tests__ (if exists — follow same style)
    - .planning/phases/999.8-multi-category-per-category-percentage-difficulty-backlog/999.8-RESEARCH.md (interface shapes under "Architecture Patterns")
  </read_first>
  <behavior>
    questions-multi.test.ts MUST assert:
    - "accepts valid multi-category output" — passes QuestionGeneratedSchema.parse with `category_slugs: ['science-and-nature']`, `category_scores: { general_knowledge: 40, science_and_nature: 70 }`
    - "rejects empty category_slugs" — parse throws when category_slugs is []
    - "rejects more than 3 non-GK slugs" — parse throws when category_slugs has 4 items
    - "rejects score > 100" — parse throws when any category_scores value is 101
    - "rejects score &lt; 0" — parse throws when any category_scores value is -1
    - "requires general_knowledge score" — app-layer guard: importing a helper `validateHasGeneralKnowledge(scores)` returns false when key missing, true when present
    - "rejects 'general-knowledge' appearing in category_slugs" — per D-13 protected constant, it is injected by pipeline not proposed by agent

    calibrator-multi.test.ts MUST assert:
    - "parses calibrator scores object" — CalibratorScoreSchema.parse accepts `{ scores: { general_knowledge: 45, science_and_nature: 68 }, reasoning: 'text' }`
    - "rejects score out of range" — parse throws when any value is 120 or -5
    - "allows all zeros" — parse accepts `{ scores: { general_knowledge: 0 }, reasoning: '' }`
    - "requires at least one score" — parse throws when scores is `{}`

    slug-converter.test.ts MUST assert:
    - "jsonKeyToSlug converts underscores to hyphens" — `jsonKeyToSlug('science_and_nature')` returns `'science-and-nature'`
    - "slugToJsonKey converts hyphens to underscores" — `slugToJsonKey('science-and-nature')` returns `'science_and_nature'`
    - "round-trips cleanly" — `slugToJsonKey(jsonKeyToSlug('a_b_c'))` returns `'a_b_c'`
    - "handles general_knowledge canonical form" — `jsonKeyToSlug('general_knowledge')` returns `'general-knowledge'`
    - "handles single-word slugs" — `jsonKeyToSlug('geography')` returns `'geography'`
  </behavior>
  <action>
    Create three vitest test files under pipeline/src with the exact assertions listed in `<behavior>`.

    File 1: `pipeline/src/agents/__tests__/questions-multi.test.ts`
    - Use `import { describe, it, expect } from 'vitest'`
    - Import `QuestionGeneratedSchema` from `../../lib/schemas` (will be extended in Wave 2; test will fail today because new fields don't exist yet)
    - Import `validateHasGeneralKnowledge` from `../../lib/general-knowledge-guard` (does not exist — Wave 2 creates it)
    - Write 7 test cases per `<behavior>`. Use realistic example data matching RESEARCH.md example: `{ general_knowledge: 22, science_and_nature: 68 }`.

    File 2: `pipeline/src/agents/__tests__/calibrator-multi.test.ts`
    - Import `CalibratorScoreSchema` from `../../lib/schemas` (does not exist yet — Wave 2 adds it)
    - Write 4 test cases per `<behavior>`.

    File 3: `pipeline/src/lib/__tests__/slug-converter.test.ts`
    - Import `jsonKeyToSlug, slugToJsonKey` from `../slug-converter` (file does not exist — Wave 2 creates it)
    - Write 5 test cases per `<behavior>`.

    Do NOT implement any source files. These tests MUST fail with "Cannot find module" or "property does not exist" errors. That failure IS the Wave 0 red state.
  </action>
  <verify>
    <automated>cd /Users/jamiepersonal/Developer/pub-quiz/pipeline &amp;&amp; npx vitest run --reporter=verbose src/agents/__tests__/questions-multi.test.ts src/agents/__tests__/calibrator-multi.test.ts src/lib/__tests__/slug-converter.test.ts 2&gt;&amp;1 | tee /tmp/999.8-01-t1.log; grep -qE "(Cannot find module|is not defined|has no exported member)" /tmp/999.8-01-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - File pipeline/src/agents/__tests__/questions-multi.test.ts exists
    - File pipeline/src/agents/__tests__/calibrator-multi.test.ts exists
    - File pipeline/src/lib/__tests__/slug-converter.test.ts exists
    - `grep -c "it(" pipeline/src/agents/__tests__/questions-multi.test.ts` returns at least 7
    - `grep -c "it(" pipeline/src/agents/__tests__/calibrator-multi.test.ts` returns at least 4
    - `grep -c "it(" pipeline/src/lib/__tests__/slug-converter.test.ts` returns at least 5
    - Test run output contains "Cannot find module" OR "has no exported member" OR similar import-failure message (proves tests reference not-yet-existing artifacts)
    - Vitest exits non-zero (red state confirmed)
  </acceptance_criteria>
  <done>Three unit test files exist, all red due to missing downstream imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test scaffolds — trigger, observed-score job, RPC</name>
  <files>pipeline/src/__tests__/question-categories-trigger.integration.test.ts, pipeline/src/__tests__/observed-score-job.test.ts, pipeline/src/__tests__/random-published-questions-rpc.integration.test.ts</files>
  <read_first>
    - pipeline/src/lib/supabase.ts (how service-role client is created)
    - pipeline/package.json (confirm vitest script, @supabase/supabase-js version)
    - supabase/migrations/00001_initial_schema.sql (categories + questions shape)
    - supabase/migrations/00005_question_plays_quiz_sessions.sql (question_plays columns for aggregation test)
    - supabase/migrations/00006_random_published_questions_rpc.sql (current RPC being replaced)
    - .planning/phases/999.8-multi-category-per-category-percentage-difficulty-backlog/999.8-RESEARCH.md (RPC pattern, trigger pattern)
  </read_first>
  <behavior>
    question-categories-trigger.integration.test.ts MUST assert:
    - "accepts question with GK row plus one extra" — insert a question row, then two question_categories rows (general-knowledge + science-and-nature), commit transaction; no error
    - "rejects question with no GK row" — insert a question_categories row pointing to a non-GK category only; expect Postgres error containing 'general-knowledge'
    - "rejects question with 5 category rows" — insert 5 rows for one question (including GK); expect error containing 'cannot have more than 4'
    - "DEFERRABLE trigger allows multi-row insert within one transaction" — open explicit BEGIN, insert non-GK row first, then GK row, COMMIT; no error (trigger must defer until commit)

    observed-score-job.test.ts MUST assert:
    - "aggregates question_plays into observed_score" — given 10 plays, 7 correct, expect observed_score = 70 and observed_n = 10
    - "upserts observed_score when row exists" — running the job twice updates the existing question_categories row (not duplicate key error)
    - "skips questions with zero plays" — questions with no question_plays rows are not touched (observed_n stays 0)
    - "aggregates per (question_id, category_id) pair" — a question tagged in 2 categories with 10 plays each yields 2 rows updated

    random-published-questions-rpc.integration.test.ts MUST assert:
    - "uses observed_score when observed_n &gt;= 30" — question A has observed_n=50, observed_score=80, estimate_score=20; querying score_min=70, score_max=100 returns A
    - "uses estimate_score when observed_n &lt; 30" — question B has observed_n=5, observed_score=80, estimate_score=20; querying score_min=70, score_max=100 does NOT return B
    - "filters by category slug" — question filed only under 'movies-and-tv' does not appear when p_category_slug='science-and-nature'
    - "returns category_slug in result rows" — each row has a category_slug column set
  </behavior>
  <action>
    Create three integration test files.

    File 1: `pipeline/src/__tests__/question-categories-trigger.integration.test.ts`
    - Skip entire suite if `process.env.SUPABASE_TEST_URL` is unset: `describe.skipIf(!process.env.SUPABASE_TEST_URL)(...)` — lets Wave 0 pass gate while actual DB run lands in Wave 1.
    - Use `createClient(SUPABASE_TEST_URL, SUPABASE_TEST_SERVICE_KEY)` from `@supabase/supabase-js`.
    - Use beforeAll to seed a question row + category rows (general-knowledge + science-and-nature).
    - Write 4 test cases per `<behavior>` using `.from('question_categories').insert(...)` for positive cases, and raw SQL via `.rpc('exec_sql', ...)` or direct insert with error catch for negative cases.
    - For DEFERRABLE test: use `.rpc('exec_sql', { sql: 'BEGIN; INSERT ... ; INSERT ... ; COMMIT;' })` — if no exec_sql RPC, document that Wave 1 must add one OR use a dedicated `supabase.sql` test helper. Mark the test `it.todo` if tooling not feasible; the skipIf above keeps the file green on CI without the env var.

    File 2: `pipeline/src/__tests__/observed-score-job.test.ts`
    - Unit-level with stub supabase client (not real DB). Use `vi.fn()` mocks for `.from().select()` and `.from().upsert()`.
    - Import `refreshObservedScores` from `../jobs/observed-score-refresh` (does not exist — Wave 3 creates it).
    - Write 4 test cases per `<behavior>`. Use hoisted mock via `vi.hoisted` per STATE.md convention "used vi.hoisted() for mock variables".

    File 3: `pipeline/src/__tests__/random-published-questions-rpc.integration.test.ts`
    - Skip entire suite if `process.env.SUPABASE_TEST_URL` is unset.
    - Use real supabase client to call `.rpc('random_published_questions_v2', ...)` (name TBD in Wave 4; test asserts contract).
    - Seed questions + question_categories rows via beforeEach, use afterEach to clean up.
    - Write 4 test cases per `<behavior>`.

    All three files MUST fail on import today because target modules do not exist. That's expected.
  </action>
  <verify>
    <automated>cd /Users/jamiepersonal/Developer/pub-quiz/pipeline &amp;&amp; npx vitest run --reporter=verbose src/__tests__/question-categories-trigger.integration.test.ts src/__tests__/observed-score-job.test.ts src/__tests__/random-published-questions-rpc.integration.test.ts 2&gt;&amp;1 | tee /tmp/999.8-01-t2.log; grep -qE "(Cannot find module|has no exported member|skipped)" /tmp/999.8-01-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - File pipeline/src/__tests__/question-categories-trigger.integration.test.ts exists
    - File pipeline/src/__tests__/observed-score-job.test.ts exists
    - File pipeline/src/__tests__/random-published-questions-rpc.integration.test.ts exists
    - `grep -c "it(" pipeline/src/__tests__/question-categories-trigger.integration.test.ts` returns at least 4
    - `grep -c "it(" pipeline/src/__tests__/observed-score-job.test.ts` returns at least 4
    - `grep -c "it(" pipeline/src/__tests__/random-published-questions-rpc.integration.test.ts` returns at least 4
    - integration files contain `describe.skipIf(!process.env.SUPABASE_TEST_URL)` or equivalent env guard
    - observed-score-job.test.ts imports `refreshObservedScores` from `../jobs/observed-score-refresh`
  </acceptance_criteria>
  <done>Three integration test scaffolds exist; either red due to missing imports OR skipped pending Wave 1+ DB setup.</done>
</task>

</tasks>

<verification>
- All six Wave 0 test files present under pipeline/src/__tests__ or pipeline/src/*/__tests__
- Running `cd pipeline && npx vitest run --reporter=verbose` exits non-zero (red) OR all Wave 0 tests are skipped with env guard and no pre-existing tests were broken
- No Wave 0 test accidentally passes against a stub — each references at least one not-yet-existing module or DB object
</verification>

<success_criteria>
- Six test files exist with concrete assertions matching the contracts in 999.8-RESEARCH.md
- Each file, when imported, triggers at least one missing-module or missing-export error (proves Wave 0 is truly red)
- Full vitest run command surfaces the red state without blowing up unrelated test suites
</success_criteria>

<output>
After completion, create `.planning/phases/999.8-multi-category-per-category-percentage-difficulty-backlog/999.8-01-SUMMARY.md`
</output>
