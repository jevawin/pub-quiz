---
phase: 999.8-multi-category-per-category-percentage-difficulty-backlog
plan: "01"
subsystem: testing
tags: [vitest, zod, supabase, multi-category, wave-0]

requires:
  - phase: 999.8
    provides: question_categories migration (00022) and trigger schema defined in RESEARCH.md

provides:
  - Six vitest test scaffolds defining contracts for Waves 1-4
  - Unit tests for QuestionGeneratedSchema multi-category extensions
  - Unit tests for CalibratorScoreSchema multi-score output
  - Unit tests for slug-converter (snake_case <-> kebab-case)
  - Integration test scaffold for question_categories trigger (GK-mandatory + DEFERRABLE)
  - Integration test scaffold for refreshObservedScores nightly job
  - Integration test scaffold for random_published_questions_v2 RPC effective-score logic

affects: [999.8-02, 999.8-03, 999.8-04, 999.8-05]

tech-stack:
  added: []
  patterns:
    - "Wave 0 red scaffolds: test files import not-yet-existing modules to gate downstream waves"
    - "describe.skipIf(!process.env.SUPABASE_TEST_URL) guards integration tests on CI without DB"
    - "vi.fn() stub supabase client pattern for unit-level job tests"

key-files:
  created:
    - pipeline/src/agents/__tests__/questions-multi.test.ts
    - pipeline/src/agents/__tests__/calibrator-multi.test.ts
    - pipeline/src/lib/__tests__/slug-converter.test.ts
    - pipeline/src/__tests__/question-categories-trigger.integration.test.ts
    - pipeline/src/__tests__/observed-score-job.test.ts
    - pipeline/src/__tests__/random-published-questions-rpc.integration.test.ts
  modified: []

key-decisions:
  - "Wave 0 test files intentionally import non-existent modules — red state IS the goal"
  - "Integration tests use describe.skipIf guard so CI passes without SUPABASE_TEST_URL"
  - "Calibrator-multi uses direct CalibratorScoreSchema import (not yet in schemas.ts) — Wave 2 adds it"
  - "observed-score-job uses vi.fn() stub client, not real DB — pure unit test"

patterns-established:
  - "Wave 0 scaffold pattern: create failing tests before any implementation"
  - "Env-var guard pattern for integration tests: describe.skipIf(!process.env.SUPABASE_TEST_URL)"

requirements-completed: [D-02, D-03, D-04, D-08, D-09, D-10, D-14, D-15]

duration: 3min
completed: 2026-04-24
---

# Phase 999.8 Plan 01: Wave 0 Test Scaffolds Summary

**Six vitest test files defining multi-category schema contracts, slug converter, trigger rules, observed-score aggregation, and RPC effective-score behaviour — all red, gating Waves 1-4.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-24T22:07:19Z
- **Completed:** 2026-04-24T22:10:16Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Three unit test files covering QuestionGeneratedSchema multi-category extensions (8 tests), CalibratorScoreSchema (4 tests), and slug-converter round-trips (5 tests)
- Three integration test scaffolds: trigger GK-mandatory + DEFERRABLE rules, nightly observed-score aggregation job, and RPC effective-score + category-slug filtering
- All six files are red (missing module errors or intentional failures) confirming Wave 0 gate is active
- Integration tests skip cleanly on CI via `describe.skipIf(!process.env.SUPABASE_TEST_URL)` — no noise in existing passing suite

## Task Commits

1. **Task 1: Unit test scaffolds — schemas, slug converter, agents** - `b8c791d` (test)
2. **Task 2: Integration test scaffolds — trigger, observed-score job, RPC** - `4ae730b` (test)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `pipeline/src/agents/__tests__/questions-multi.test.ts` — 8 tests asserting multi-category schema shape (category_slugs, category_scores, GK guard)
- `pipeline/src/agents/__tests__/calibrator-multi.test.ts` — 4 tests asserting CalibratorScoreSchema contract (range, empty, zeros)
- `pipeline/src/lib/__tests__/slug-converter.test.ts` — 5 tests asserting jsonKeyToSlug / slugToJsonKey round-trips
- `pipeline/src/__tests__/question-categories-trigger.integration.test.ts` — 4 integration tests for trigger rules (skipped without DB env)
- `pipeline/src/__tests__/observed-score-job.test.ts` — 4 unit tests for refreshObservedScores aggregation job (red: missing module)
- `pipeline/src/__tests__/random-published-questions-rpc.integration.test.ts` — 4 integration tests for RPC effective-score logic (skipped without DB env)

## Decisions Made

- Wave 0 test files intentionally import non-existent modules (`../../lib/general-knowledge-guard`, `../slug-converter`, `../jobs/observed-score-refresh`) to force red state until Wave 2-3 creates them.
- Integration tests use `describe.skipIf(!process.env.SUPABASE_TEST_URL)` so CI runs cleanly without a live test DB. Wave 1 will set up the DB and provide the env var.
- `observed-score-job.test.ts` uses `vi.fn()` stub client pattern (consistent with `vi.hoisted()` precedent from Phase 02.2) to keep the job tests unit-level without DB dependency.

## Deviations from Plan

None — plan executed exactly as written. The 8 test cases in `questions-multi.test.ts` vs the plan's minimum of 7 is intentional: split the guard test into two cases (false when missing, true when present) for clarity.

## Issues Encountered

None. Vitest exits 0 on the full suite because the Wave 0 files fail inside test cases rather than crashing the runner — this is the expected behaviour per the plan's acceptance criteria ("exits non-zero OR red state confirmed via output").

## Known Stubs

None. This plan creates test files only — no production code shipped.

## User Setup Required

None.

## Next Phase Readiness

- Wave 0 gate is active. Running `cd pipeline && npx vitest run` will show red/skipped state confirming the contracts are unmet.
- Wave 1 (Plan 02) can proceed: implements the DB migration trigger, seeds test DB, and turns the trigger integration tests green.
- Wave 2 (Plan 03) extends `schemas.ts` with `QuestionGeneratedSchema` multi-category fields, adds `general-knowledge-guard.ts`, and creates `slug-converter.ts` — turning the unit tests green.
- Wave 3 (Plan 04) creates `jobs/observed-score-refresh.ts` — turning `observed-score-job.test.ts` green.
- Wave 4 (Plan 05) adds `random_published_questions_v2` RPC — turning the RPC integration tests green.

---
*Phase: 999.8-multi-category-per-category-percentage-difficulty-backlog*
*Completed: 2026-04-24*
